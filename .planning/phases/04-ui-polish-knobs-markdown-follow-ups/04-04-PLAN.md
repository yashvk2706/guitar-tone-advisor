---
phase: 04-ui-polish-knobs-markdown-follow-ups
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - frontend/components/FollowUpRail.tsx
  - frontend/components/MessageBubble.tsx
  - frontend/components/ChatPage.tsx
autonomous: true
requirements:
  - CHAT-04

must_haves:
  truths:
    - "Three follow-up buttons appear under the latest completed assistant message: 'Cleaner?', 'Live setting?', 'Budget version?'"
    - "Clicking a follow-up button submits that button's text as a new user turn"
    - "The follow-up rail does NOT appear during streaming (isStreaming must be false)"
    - "The follow-up rail does NOT appear on any message that is not the latest assistant message"
    - "After clicking a follow-up button, the rail disappears (the new assistant message is now streaming)"
    - "npm run build exits 0 — final phase build check"
  artifacts:
    - path: "frontend/components/FollowUpRail.tsx"
      provides: "Three fixed follow-up buttons, calls onSubmit(label) on click"
      exports: ["FollowUpRail (default)"]
    - path: "frontend/components/MessageBubble.tsx"
      provides: "FollowUpRail rendered when isLatestAssistant && citations !== undefined && !isStreaming"
      contains: "FollowUpRail"
    - path: "frontend/components/ChatPage.tsx"
      provides: "isLatestAssistant prop computed, onFollowUp wired to handleSubmit(overrideMessage)"
      contains: "isLatestAssistant"
  key_links:
    - from: "ChatPage.tsx messages.map()"
      to: "MessageBubble isLatestAssistant prop"
      via: "msg.role === 'assistant' && index === lastAssistantIndex"
      pattern: "isLatestAssistant"
    - from: "MessageBubble onFollowUp prop"
      to: "FollowUpRail onSubmit prop"
      via: "<FollowUpRail onSubmit={onFollowUp} />"
      pattern: "FollowUpRail"
    - from: "FollowUpRail onSubmit"
      to: "ChatPage handleSubmit(overrideMessage)"
      via: "ChatPage passes (text) => handleSubmit(text) as onFollowUp to MessageBubble"
      pattern: "handleSubmit"
---

<objective>
Create FollowUpRail.tsx and wire it into MessageBubble and ChatPage so that three
fixed follow-up buttons ("Cleaner?", "Live setting?", "Budget version?") appear under
the latest completed assistant message and submit their text on click.

Purpose: Closes CHAT-04 — the final Phase 4 requirement. This is the last plan and
includes the final `npm run build` verification for the whole phase.

Output: New `frontend/components/FollowUpRail.tsx`; updated MessageBubble.tsx (activate
the isLatestAssistant and onFollowUp props declared in Plan 03); updated ChatPage.tsx
(compute isLatestAssistant, pass onFollowUp callback). Passing `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-CONTEXT.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-02-SUMMARY.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-03-SUMMARY.md

<interfaces>
<!-- Interfaces from prior plans that this plan builds on -->

From frontend/components/MessageBubble.tsx (after Plans 01+02+03):
  MessageBubbleProps (as declared in Plan 03 Task 2):
    message: Message
    onCitationClick: (chunkId: string) => void
    loadingLabel?: string               — wired in Plan 03
    isLatestAssistant?: boolean         — declared in Plan 03, NOT yet used; this plan activates it
    onFollowUp?: (text: string) => void — declared in Plan 03, NOT yet used; this plan activates it

  Post-stream gate (existing invariant, do not change):
    message.citations !== undefined && !message.isStreaming

  Current element ordering inside assistant bubble (after Plans 01+02+03):
    1. Copy button (absolute)
    2. ReactMarkdown content div
    3. Loading indicator
    4. Knob row (from Plan 02)
    5. CoverageIndicator
    6. Citation pills
    7. {/* FollowUpRail placeholder comment — this plan fills it */}

From frontend/components/ChatPage.tsx (after Plan 03):
  handleSubmit: useCallback(async (overrideMessage?: string) => { ... })
  messages: Message[] — need to find last assistant index for isLatestAssistant

From .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md:
  §6 FollowUpRail component contract — exact button labels, classNames, div wrapper
  §Interaction Contracts — follow-up button click flow (5-step sequence)
  §Copywriting — exact label strings: "Cleaner?", "Live setting?", "Budget version?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FollowUpRail.tsx component</name>
  <files>frontend/components/FollowUpRail.tsx</files>

  <read_first>
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §6 (FollowUpRail full component contract)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §Copywriting (exact button labels)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §Color (follow-up pill colors)
    - frontend/components/CitationPill.tsx (pill pattern reference for horizontal row layout)
  </read_first>

  <action>
    Create `frontend/components/FollowUpRail.tsx`.

    Declare a module-level const for the three button labels:
      const FOLLOW_UP_LABELS = ['Cleaner?', 'Live setting?', 'Budget version?'] as const;

    Props interface:
      interface FollowUpRailProps { onSubmit: (text: string) => void; }

    Default export: function FollowUpRail({ onSubmit }: FollowUpRailProps)

    Return JSX:
      <div className="flex flex-row flex-wrap gap-2 mt-2">
        {FOLLOW_UP_LABELS.map((label) => (
          <button
            key={label}
            onClick={() => onSubmit(label)}
            className="h-7 px-3 rounded-full text-xs font-semibold bg-zinc-800 text-zinc-400 border border-zinc-700 hover:bg-zinc-700 hover:text-zinc-50 hover:border-zinc-600 transition-colors focus:outline-none focus:ring-1 focus:ring-blue-500 focus:ring-offset-1 focus:ring-offset-zinc-950"
          >
            {label}
          </button>
        ))}
      </div>

    No state. No imports other than React (and React is implicit in JSX in Next.js App Router).
    No `type` on the button elements needed — they default to "button" in React, but add
    `type="button"` explicitly to prevent accidental form submission in any future wrapper.

    EXACT button labels (copy-sensitive — must match UI-SPEC §Copywriting exactly):
      "Cleaner?" — note the question mark
      "Live setting?" — note the question mark
      "Budget version?" — note the question mark
  </action>

  <verify>
    <automated>grep -n "Cleaner\|Live setting\|Budget version" /Users/yashvinaykumar/Desktop/guitar-tone-advisor/frontend/components/FollowUpRail.tsx</automated>
  </verify>

  <done>
    - `frontend/components/FollowUpRail.tsx` exists and exports default FollowUpRail
    - FOLLOW_UP_LABELS contains exactly: 'Cleaner?', 'Live setting?', 'Budget version?'
    - Each button has `type="button"` and calls `onSubmit(label)` on click
    - Button className matches UI-SPEC §6: `h-7 px-3 rounded-full text-xs font-semibold bg-zinc-800 text-zinc-400 border border-zinc-700 hover:bg-zinc-700 hover:text-zinc-50 hover:border-zinc-600 transition-colors focus:outline-none focus:ring-1 focus:ring-blue-500 focus:ring-offset-1 focus:ring-offset-zinc-950`
    - No external imports
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire FollowUpRail into MessageBubble and ChatPage; final build check</name>
  <files>frontend/components/MessageBubble.tsx, frontend/components/ChatPage.tsx</files>

  <read_first>
    - frontend/components/MessageBubble.tsx — full file as left by Plans 01+02+03 (must read before editing)
    - frontend/components/ChatPage.tsx — full file as left by Plan 03 (must read before editing)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §6 (render condition, placement in bubble)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §Interaction Contracts (follow-up click flow)
    - frontend/AGENTS.md — Next.js version caveats
  </read_first>

  <action>
    == MessageBubble.tsx changes ==

    Step 1 — Add FollowUpRail import:
      import FollowUpRail from '@/components/FollowUpRail';

    Step 2 — Activate isLatestAssistant and onFollowUp in the function signature:
    These props are already declared in MessageBubbleProps (Plan 03). Just add them to the
    destructuring:
      export default function MessageBubble({ message, onCitationClick, loadingLabel, isLatestAssistant, onFollowUp }: MessageBubbleProps)

    Step 3 — Add FollowUpRail render at the bottom of the post-stream gate block:
    Locate the comment placeholder left by Plan 03 (`{/* FollowUpRail — Plan 04 inserts here */}`)
    inside the assistant bubble, after the citation pills row. Replace it with:

      {isLatestAssistant && message.citations !== undefined && !message.isStreaming && onFollowUp && (
        <FollowUpRail onSubmit={onFollowUp} />
      )}

    Render condition breakdown:
    - isLatestAssistant: only the last assistant message
    - message.citations !== undefined: post-stream gate (D-08 invariant)
    - !message.isStreaming: post-stream gate (D-08 invariant)
    - onFollowUp: prop is optional — only render if callback is provided (avoids TypeScript
      undefined error and makes the component safe when used standalone in tests)

    == ChatPage.tsx changes ==

    Step 4 — Compute lastAssistantIndex in the messages.map() render:
    Before the messages.map() call (or inline using reduce), find the index of the last
    assistant message:
      const lastAssistantIndex = messages.reduce(
        (last, msg, idx) => (msg.role === 'assistant' ? idx : last),
        -1
      );

    This is a pure array scan (O(n)) on the messages array, which is fine for session-length
    arrays. Do NOT memoize this — it is not worth the complexity for <100 messages.

    Step 5 — Pass isLatestAssistant and onFollowUp to MessageBubble in the map:
    Update the messages.map() block:

      {messages.map((msg, index) => {
        const loadingLabel = (msg.isStreaming && streamPhase !== 'idle')
          ? (streamPhase === 'searching' ? 'Searching corpus...' : 'Drafting...')
          : undefined;
        return (
          <MessageBubble
            key={msg.id}
            message={msg}
            onCitationClick={handleCitationClick}
            loadingLabel={loadingLabel}
            isLatestAssistant={msg.role === 'assistant' && index === lastAssistantIndex}
            onFollowUp={(text) => handleSubmit(text)}
          />
        );
      })}

    The `index` parameter: messages.map receives (msg, index) — add the index parameter.
    `isLatestAssistant` is `true` only when msg.role === 'assistant' AND index is the last
    assistant message index. For user messages and non-latest assistant messages, it is false.

    Step 6 — Follow-up click flow verification (per UI-SPEC §Interaction Contracts):
    When user clicks "Cleaner?":
    1. FollowUpRail.onSubmit("Cleaner?") fires
    2. MessageBubble.onFollowUp("Cleaner?") fires
    3. ChatPage calls handleSubmit("Cleaner?") — overrideMessage = "Cleaner?"
    4. handleSubmit reads: message = "Cleaner?" (overrideMessage wins over inputValue)
    5. guard passes (message is truthy, isStreaming is false)
    6. Does NOT clear textarea (overrideMessage !== undefined branch)
    7. Appends user message { role: "user", content: "Cleaner?" } to messages
    8. Appends new streaming assistant message
    9. Calls streamChat("Cleaner?", ...)
    10. The follow-up rail disappears: the new latest assistant message is streaming,
        so isStreaming=true suppresses the FollowUpRail render gate

    This flow is automatic from the handleSubmit refactor in Plan 03 — verify the
    overrideMessage branch was correctly implemented.

    Step 7 — Final npm run build:
    Run `npm run build` in `frontend/`. This is the final build check for the entire Phase 4.
    All four plans' changes must compile cleanly together.
    Fix any remaining TypeScript errors:
    - Unused variable warnings for `isLatestAssistant` / `onFollowUp` (if Plan 03 added
      `void` guards): remove the guards now that the variables are used.
    - Index parameter in messages.map: ensure the map callback signature is `(msg, index)`.
    - FollowUpRail import path: `@/components/FollowUpRail` — verify this resolves correctly.
  </action>

  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor/frontend && npm run build 2>&1</automated>
  </verify>

  <done>
    - MessageBubble.tsx imports FollowUpRail from '@/components/FollowUpRail'
    - MessageBubble renders `<FollowUpRail onSubmit={onFollowUp} />` gated on `isLatestAssistant && message.citations !== undefined && !message.isStreaming && onFollowUp`
    - ChatPage.tsx computes `lastAssistantIndex` via reduce before the map
    - ChatPage.tsx passes `isLatestAssistant={msg.role === 'assistant' && index === lastAssistantIndex}` to MessageBubble
    - ChatPage.tsx passes `onFollowUp={(text) => handleSubmit(text)}` to MessageBubble
    - `npm run build` exits 0 — entire Phase 4 compiles cleanly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Follow-up button click → handleSubmit | Fixed string from FOLLOW_UP_LABELS is submitted as user message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | Follow-up button labels | accept | Labels are compile-time constants from FOLLOW_UP_LABELS — no user-controlled input injection possible. The strings "Cleaner?", "Live setting?", "Budget version?" are fixed. |
| T-04-08 | Tampering | handleSubmit overrideMessage | accept | overrideMessage only comes from FollowUpRail's fixed labels or future callers in the same codebase. No external input reaches overrideMessage. Single-user local tool. |
</threat_model>

<verification>
Overall Phase 4 verification — run these after Plan 04 completes:

1. `grep "FollowUpRail" frontend/components/MessageBubble.tsx | wc -l` returns >= 2 (import + render)
2. `grep "isLatestAssistant" frontend/components/ChatPage.tsx | wc -l` returns >= 2 (computation + prop)
3. `grep "lastAssistantIndex" frontend/components/ChatPage.tsx` returns the reduce line
4. `grep -r "dangerouslySetInnerHTML" frontend/components/` returns empty (T-03-14 final check)
5. `grep "react-markdown" frontend/package.json` returns the version line
6. `grep "streamPhase" frontend/components/ChatPage.tsx | wc -l` returns >= 5
7. `grep "navigator.clipboard" frontend/components/MessageBubble.tsx` returns handleCopy line
8. `cd frontend && npm run build` exits 0 — final phase build check
</verification>

<success_criteria>
- FollowUpRail.tsx: three fixed buttons, correct labels, correct className, type="button"
- MessageBubble.tsx: FollowUpRail rendered under latest completed assistant message only (isLatestAssistant + D-08 gate)
- ChatPage.tsx: lastAssistantIndex computed, isLatestAssistant and onFollowUp passed correctly
- Follow-up click flow: button click → handleSubmit(label) → new user + assistant turn appended
- No dangerouslySetInnerHTML anywhere in frontend/components/ (T-03-14 invariant)
- npm run build passes — all Phase 4 changes compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-04-SUMMARY.md`
</output>

---
phase: 04-ui-polish-knobs-markdown-follow-ups
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/components/ChatPage.tsx
  - frontend/components/MessageBubble.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "While a response is streaming, the assistant bubble shows 'Searching corpus...' then transitions to 'Drafting...' after the first token arrives"
    - "The loading indicator disappears when onDone fires (citations received, isStreaming becomes false)"
    - "Each assistant bubble has a copy button (top-right, opacity-0 group-hover:opacity-100) that copies the raw Markdown text to clipboard and shows a checkmark for 2 seconds"
    - "streamPhase state transitions: idle → searching (on submit) → drafting (on first token) → idle (after onDone + 300ms)"
    - "New Chat resets streamPhase to idle immediately"
    - "npm run build exits 0"
  artifacts:
    - path: "frontend/components/ChatPage.tsx"
      provides: "streamPhase state machine, loadingLabel prop passed to streaming message"
      contains: "streamPhase"
    - path: "frontend/components/MessageBubble.tsx"
      provides: "copy button with copied state, loadingLabel prop rendering"
      contains: "loadingLabel"
  key_links:
    - from: "ChatPage.tsx streamPhase state"
      to: "MessageBubble loadingLabel prop"
      via: "loadingLabel prop passed only to the currently-streaming message"
      pattern: "loadingLabel"
    - from: "MessageBubble copy button"
      to: "navigator.clipboard.writeText"
      via: "handleCopy calls navigator.clipboard.writeText(message.content)"
      pattern: "navigator.clipboard"
---

<objective>
Add the streamPhase loading state machine to ChatPage.tsx and wire it to MessageBubble
via a `loadingLabel` prop; add the copy-to-clipboard button with 2s success feedback to
MessageBubble.tsx.

Purpose: Closes UI-04 (labeled loading state transitions) and UI-03 (one-click copy).
This plan runs in Wave 2 in parallel with Plan 02 (rotary knobs) — they touch different
areas of the two files (ChatPage state + MessageBubble loading/copy vs. MessageBubble
knob row only).

Output: ChatPage.tsx with streamPhase state and handleSubmit refactored to accept
optional overrideMessage; MessageBubble.tsx with loadingLabel prop, copy button, and
Copy/Check lucide imports. npm run build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-CONTEXT.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs -->

From frontend/components/ChatPage.tsx (current state — read the full file before editing):
  Existing state: sessionId, messages, isStreaming, inputValue, drawer
  handleSubmit: useCallback(async () => { ... }, [inputValue, isStreaming, sessionId])
    - currently reads from `inputValue.trim()` directly
    - needs refactor to accept optional overrideMessage?: string
  streamChat callbacks: onSession, onToken, onCitations, onError, onDone
    - onToken: first token fires → searching → drafting transition
    - onDone: stream complete → reset streamPhase to idle after 300ms

From frontend/components/MessageBubble.tsx (after Plan 01 changes):
  MessageBubbleProps currently: { message: Message; onCitationClick: (chunkId: string) => void }
  This plan adds: loadingLabel?: string (optional, only passed for currently-streaming message)

  Current assistant bubble outer div:
    <div className={`max-w-[80%] bg-zinc-900 rounded-2xl rounded-tl-sm px-4 py-3 ...`}>
  Must add: `relative group` to this div for copy button absolute positioning

From .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md:
  §2 streamPhase loading state machine — full transition table
  §7 Copy button markup — exact className, icon sizes, state behavior
  §Copywriting — exact strings: "Searching corpus...", "Drafting...", "Copy response", "Copied!"

From frontend/hooks/useSSEStream.ts:
  onToken callback: called for each token; first call signals searching→drafting transition
  onDone callback: called after stream completes (after citations or on error cleanup)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add streamPhase state machine to ChatPage.tsx and refactor handleSubmit</name>
  <files>frontend/components/ChatPage.tsx</files>

  <read_first>
    - frontend/components/ChatPage.tsx — full file (read before editing; note all existing useCallback deps)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §2 (streamPhase transition table and loadingLabel prop)
    - frontend/hooks/useSSEStream.ts — onToken and onDone callback signatures
  </read_first>

  <behavior>
    - On submit: streamPhase transitions from 'idle' to 'searching' before streamChat is called
    - On first onToken call: streamPhase transitions from 'searching' to 'drafting'
    - On onDone: streamPhase resets to 'idle' after 300ms (setTimeout 300)
    - On onError: streamPhase resets to 'idle' immediately
    - On New Chat: streamPhase resets to 'idle' immediately
    - handleSubmit(overrideMessage?: string): when overrideMessage is provided, uses that string instead of inputValue.trim(); does NOT clear textarea or update inputValue when override is provided
    - The loadingLabel passed to the streaming assistant message is: streamPhase === 'searching' ? 'Searching corpus...' : streamPhase === 'drafting' ? 'Drafting...' : undefined
    - loadingLabel is only passed to the message that is currently streaming (identified by assistantId)
  </behavior>

  <action>
    Step 1 — Add streamPhase state:
    In ChatPage.tsx, add after the existing `isStreaming` state declaration:
      const [streamPhase, setStreamPhase] = useState<'idle' | 'searching' | 'drafting' | 'done'>('idle');

    Step 2 — Refactor handleSubmit to accept overrideMessage:
    Change the handleSubmit signature to:
      const handleSubmit = useCallback(async (overrideMessage?: string) => {
    At the top of handleSubmit, replace:
      const message = inputValue.trim();
    with:
      const message = overrideMessage !== undefined ? overrideMessage : inputValue.trim();
    The guard `if (!message || isStreaming) return;` remains.
    When overrideMessage is provided: skip `setInputValue("")` and the textarea height reset —
    only clear textarea when the user typed the message themselves (overrideMessage === undefined).

    Updated handleSubmit guard block:
      if (overrideMessage === undefined) {
        setInputValue("");
        if (textareaRef.current) { textareaRef.current.style.height = "auto"; }
      }

    Step 3 — Add streamPhase transitions inside handleSubmit:
    Before calling `streamChat(...)`, add:
      setStreamPhase('searching');

    Inside the `onToken` callback, add a one-time transition. Because onToken fires for
    every token, use a ref to track whether the first token has fired:
      const hasFirstTokenRef = useRef(false);
    Initialize: reset it at the start of handleSubmit: `hasFirstTokenRef.current = false;`
    Inside onToken callback:
      if (!hasFirstTokenRef.current) {
        hasFirstTokenRef.current = true;
        setStreamPhase('drafting');
      }

    Inside the `onDone` callback, replace or augment with:
      setTimeout(() => setStreamPhase('idle'), 300);
    (The existing setIsStreaming(false) and textareaRef.current?.focus() remain.)

    Inside the `onError` callback:
      setStreamPhase('idle');
    (alongside existing setIsStreaming(false))

    Step 4 — Compute loadingLabel and pass it to the streaming message:
    In the messages.map() render inside ChatPage's JSX, compute a derived label per message:

      {messages.map((msg) => {
        const loadingLabel = (msg.isStreaming && streamPhase !== 'idle')
          ? (streamPhase === 'searching' ? 'Searching corpus...' : 'Drafting...')
          : undefined;
        return (
          <MessageBubble
            key={msg.id}
            message={msg}
            onCitationClick={handleCitationClick}
            loadingLabel={loadingLabel}
          />
        );
      })}

    Step 5 — Update New Chat handler:
    In handleNewChat, add:
      setStreamPhase('idle');

    Step 6 — Update handleSubmit dependency array:
    The useCallback deps for handleSubmit currently are [inputValue, isStreaming, sessionId].
    After refactor, no new state is READ inside handleSubmit (streamPhase is only SET, not read).
    hasFirstTokenRef is a ref (stable). Dependencies remain [inputValue, isStreaming, sessionId].

    Step 7 — Update the onClick of the send button:
    The button currently calls `onClick={handleSubmit}`. After refactor, handleSubmit accepts
    an optional first argument. The button should call `() => handleSubmit()` (no override)
    to keep the signature clean, OR keep `onClick={handleSubmit}` since React button onClick
    passes a SyntheticEvent as the first arg — which is not a string and not `undefined`.
    IMPORTANT: Since handleSubmit now accepts `overrideMessage?: string`, a SyntheticEvent
    passed as the first arg would be used as the message string, which is wrong.
    Fix: change the button to `onClick={() => handleSubmit()}` (explicit no-arg call).
    Also update the Enter-key handler: `handleSubmit()` (already called with no args — confirm).

    Step 8 — Build check:
    Run `npm run build` in frontend/. TypeScript will catch type errors in the new
    overrideMessage parameter and loadingLabel prop (MessageBubble will error until Task 2
    updates MessageBubbleProps — build Task 2 first or accept TS error, then fix together).
    Recommended: do both tasks in sequence and build once at the end.
  </action>

  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor/frontend && grep -n "streamPhase" src/components/ChatPage.tsx 2>/dev/null || grep -n "streamPhase" components/ChatPage.tsx 2>/dev/null || grep -rn "streamPhase" . --include="*.tsx" | grep ChatPage</automated>
  </verify>

  <done>
    - ChatPage.tsx contains `streamPhase` state: `useState<'idle' | 'searching' | 'drafting' | 'done'>('idle')`
    - handleSubmit accepts optional `overrideMessage?: string` parameter
    - setStreamPhase('searching') called at start of handleSubmit
    - hasFirstTokenRef.current tracks first token; setStreamPhase('drafting') on first token
    - setStreamPhase('idle') called in onDone (via setTimeout 300ms) and onError (immediate)
    - handleNewChat calls setStreamPhase('idle')
    - loadingLabel computed per message and passed to MessageBubble
    - Send button calls `() => handleSubmit()` (no SyntheticEvent leak)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add loadingLabel prop and copy button to MessageBubble.tsx</name>
  <files>frontend/components/MessageBubble.tsx</files>

  <read_first>
    - frontend/components/MessageBubble.tsx — full file as left by Plan 01 (must read before editing)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §7 (copy button markup, exact classNames, opacity pattern)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §2 (loading indicator placement and markup)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §Copywriting (exact strings)
    - frontend/components/CoverageIndicator.tsx (mt-1 pattern — loading indicator uses same bottom-margin)
  </read_first>

  <action>
    Step 1 — Update imports in MessageBubble.tsx:
    Add `useState` to the React import (currently none — this component has no state; add it).
    If lucide-react is not yet imported, add: `import { Copy, Check } from 'lucide-react';`
    If already imported (from Plan 01), add Copy and Check to the existing import.

    Step 2 — Update MessageBubbleProps:
    Add two new optional props:
      interface MessageBubbleProps {
        message: Message;
        onCitationClick: (chunkId: string) => void;
        loadingLabel?: string;       // "Searching corpus..." | "Drafting..." | undefined
        isLatestAssistant?: boolean; // Plan 04 will use this — declare now to avoid a second edit
        onFollowUp?: (text: string) => void; // Plan 04 will use this — declare now
      }
    Update the function signature destructuring accordingly.

    NOTE: isLatestAssistant and onFollowUp will not be USED in this plan — only declared
    in the props interface so Plan 04 can wire them without touching the interface again.
    This prevents a merge conflict if Plans 03 and 04 both edit MessageBubbleProps.

    Step 3 — Add copied state:
    Inside the MessageBubble function body (assistant branch only), add:
      const [copied, setCopied] = useState(false);

    Step 4 — Add handleCopy function:
    Inside the assistant branch:
      const handleCopy = () => {
        navigator.clipboard.writeText(message.content).then(() => {
          setCopied(true);
          setTimeout(() => setCopied(false), 2000);
        });
      };

    Step 5 — Add `relative group` to the assistant bubble outer div:
    The outer div currently is:
      <div className={`max-w-[80%] bg-zinc-900 rounded-2xl rounded-tl-sm px-4 py-3 text-sm leading-relaxed text-zinc-50 ${streamingClass}`}>
    Update to include `relative group`:
      <div className={`relative group max-w-[80%] bg-zinc-900 rounded-2xl rounded-tl-sm px-4 py-3 text-sm leading-relaxed text-zinc-50 ${streamingClass}`}>

    Step 6 — Add copy button inside the outer div (first child, before ReactMarkdown content):
    Position it absolutely, top-right corner, visible only on hover:
      <button
        onClick={handleCopy}
        className="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity h-6 w-6 flex items-center justify-center rounded text-zinc-700 hover:text-zinc-400 hover:bg-zinc-800"
        title={copied ? "Copied!" : "Copy response"}
        aria-label={copied ? "Copied!" : "Copy response"}
      >
        {copied ? <Check size={14} className="text-green-500" /> : <Copy size={14} />}
      </button>

    Step 7 — Add loading indicator row:
    Inside the assistant bubble, after the ReactMarkdown content div and before the
    post-stream gate block, insert:
      {message.isStreaming && loadingLabel && (
        <p className="text-xs text-zinc-500 mt-1 animate-pulse">{loadingLabel}</p>
      )}
    This appears and disappears with the streaming state. When loadingLabel is undefined
    (streamPhase is 'idle' or 'done'), nothing renders.

    Step 8 — Verify final element ordering in the assistant bubble:
    The outer div should contain in order:
      1. Copy button (absolute positioned — does not affect flow)
      2. `<div className="space-y-1"><ReactMarkdown ...>{message.content}</ReactMarkdown></div>`
      3. `{message.isStreaming && loadingLabel && <p ...>{loadingLabel}</p>}`
      4. `{/* Knob row — added by Plan 02 */}` (do not disturb)
      5. Post-stream gate block: CoverageIndicator, citation pills (do not disturb)
      6. `{/* FollowUpRail — Plan 04 inserts here */}` (comment placeholder)

    Step 9 — Build verification:
    Run `npm run build` in `frontend/`. Fix TypeScript errors — common: `useState` not
    imported, lucide Copy/Check not imported, isLatestAssistant/onFollowUp props declared
    but not yet used (TS unused variable warning — these are intentionally forward-declared;
    add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` if needed, or
    simply reference them with a void expression like `void isLatestAssistant` to satisfy
    the compiler until Plan 04 uses them).
  </action>

  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>

  <done>
    - MessageBubbleProps declares loadingLabel?: string, isLatestAssistant?: boolean, onFollowUp?: (text: string) => void
    - MessageBubble imports Copy and Check from lucide-react
    - MessageBubble has `const [copied, setCopied] = useState(false)` in the assistant branch
    - handleCopy calls navigator.clipboard.writeText(message.content) and sets copied for 2s
    - The outer assistant bubble div has `relative group` in its className
    - Copy button: `absolute top-2 right-2 opacity-0 group-hover:opacity-100`, shows Check when copied
    - Loading indicator: `{message.isStreaming && loadingLabel && <p className="text-xs text-zinc-500 mt-1 animate-pulse">{loadingLabel}</p>}`
    - `npm run build` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard API | navigator.clipboard.writeText writes model-generated text to system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | navigator.clipboard | accept | Single-user local tool; user explicitly clicks Copy. The text written is message.content, which is model output already visible in the UI. No PII injected by the system. |
| T-04-06 | Tampering | streamPhase transitions | accept | Pure UI state machine with no backend calls. The only effect of a wrong phase is an incorrect loading label string — no security impact. |
</threat_model>

<verification>
1. `grep -n "streamPhase" frontend/components/ChatPage.tsx | wc -l` returns >= 5 (declaration + set calls + passed to prop)
2. `grep "loadingLabel" frontend/components/MessageBubble.tsx | wc -l` returns >= 2 (prop + render)
3. `grep "navigator.clipboard" frontend/components/MessageBubble.tsx` returns the handleCopy line
4. `grep "relative group" frontend/components/MessageBubble.tsx` returns the outer div
5. `grep "overrideMessage" frontend/components/ChatPage.tsx` returns the parameter usage
6. `cd frontend && npm run build` exits 0
</verification>

<success_criteria>
- streamPhase state machine: idle → searching → drafting → idle cycle wired to SSE callbacks
- loadingLabel prop flows from ChatPage to MessageBubble, displaying "Searching corpus..." then "Drafting..."
- Copy button: opacity-0 by default, visible on hover, writes raw Markdown, shows Check for 2s
- handleSubmit accepts optional overrideMessage to support Plan 04 follow-up button clicks
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-03-SUMMARY.md`
</output>

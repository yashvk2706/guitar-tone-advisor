---
phase: 04-ui-polish-knobs-markdown-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/components/MessageBubble.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Markdown in assistant responses renders as styled HTML (bold, lists, code blocks, blockquotes) — not raw asterisks or backticks"
    - "Streaming messages display the ▋ cursor on the outer bubble wrapper, not inside a <p> or <li> rendered by react-markdown"
    - "No dangerouslySetInnerHTML is used — react-markdown JSX mode satisfies T-03-14"
    - "npm run build exits 0 in the frontend/ directory after the change"
  artifacts:
    - path: "frontend/package.json"
      provides: "react-markdown@10.1.0 dependency declaration"
      contains: "react-markdown"
    - path: "frontend/components/MessageBubble.tsx"
      provides: "ReactMarkdown render with MARKDOWN_COMPONENTS mapping"
      exports: ["MessageBubble", "Message"]
  key_links:
    - from: "frontend/components/MessageBubble.tsx"
      to: "react-markdown"
      via: "import ReactMarkdown from 'react-markdown'"
      pattern: "import ReactMarkdown"
    - from: "MessageBubble assistant branch"
      to: "MARKDOWN_COMPONENTS const"
      via: "<ReactMarkdown components={MARKDOWN_COMPONENTS}>"
      pattern: "MARKDOWN_COMPONENTS"
---

<objective>
Install react-markdown v10.1.0 and wire it into MessageBubble.tsx so that assistant
responses render styled Markdown instead of raw text.

Purpose: Replaces the plain `{message.content}` text render in the assistant bubble
with `<ReactMarkdown components={MARKDOWN_COMPONENTS}>` so bold, lists, code fences,
blockquotes, and headings all render correctly — streaming-safe because react-markdown
v10 accepts a partial, growing string.

Output: Updated package.json (react-markdown listed), updated MessageBubble.tsx with
MARKDOWN_COMPONENTS const and ReactMarkdown render, passing `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-CONTEXT.md
@.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md
@.planning/phases/03-grounded-generation-minimal-chat-ui/03-CONTEXT.md

<interfaces>
<!-- Current MessageBubble.tsx — executor must read the full file before editing -->
<!-- Key interfaces extracted for reference: -->

From frontend/components/MessageBubble.tsx:

export interface Message {
  id: string;
  role: "user" | "assistant" | "error";
  content: string;
  citations?: CitationSource[];  // set after event: citations fires (D-08)
  isStreaming?: boolean;
}

interface MessageBubbleProps {
  message: Message;
  onCitationClick: (chunkId: string) => void;
}

// Current assistant render (lines 52-77) — this is what gets replaced:
// streamingClass applied to outer div (after:content-['▋'] after:animate-pulse after:text-zinc-400)
// {message.content} is raw text — replace with <ReactMarkdown>
// post-stream gate: message.citations !== undefined && !message.isStreaming
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install react-markdown and wire into MessageBubble</name>
  <files>frontend/package.json, frontend/components/MessageBubble.tsx</files>

  <read_first>
    - frontend/components/MessageBubble.tsx — full current file (read before editing)
    - frontend/package.json — current deps (react 19.2.4, no react-markdown yet)
    - .planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-UI-SPEC.md §Typography (Markdown element → Tailwind mapping table) and §Component Contracts §1
    - frontend/AGENTS.md and frontend/CLAUDE.md — Next.js version caveats
    - frontend/node_modules/next/dist/docs/index.md — scan for any breaking changes relevant to React 19 + Tailwind 4
  </read_first>

  <behavior>
    - parseKnobs("Bass=7 Mid=4") is not tested here (that is Plan 2). Test this plan's output only.
    - Build passes: `npm run build` in frontend/ exits 0.
    - MARKDOWN_COMPONENTS const is exported or module-level in MessageBubble.tsx.
    - ReactMarkdown renders bold as `<strong className="font-semibold text-zinc-50">`.
    - No `rehype-raw` or `remark-gfm` plugin is passed to ReactMarkdown.
  </behavior>

  <action>
    Step 1 — Install the dependency:
    Run `npm install react-markdown@10.1.0` inside `frontend/`. This adds the entry to
    `package.json` `dependencies` and populates `node_modules/react-markdown`.

    Step 2 — Add MARKDOWN_COMPONENTS const to MessageBubble.tsx:
    Above the component function, declare `const MARKDOWN_COMPONENTS` as a module-level
    const (type `import type { Components } from 'react-markdown'` if needed, or plain
    object typed inline). Map each HTML element to a component function that applies
    Tailwind classes per UI-SPEC §Typography table:

    Element→class mapping (exact, from UI-SPEC §Typography):
    - strong → `font-semibold text-zinc-50`
    - em → `italic text-zinc-200`
    - h1 → `text-base font-semibold text-zinc-50 mt-3 mb-1`
    - h2 → `text-sm font-semibold text-zinc-200 mt-3 mb-1`
    - h3 → `text-sm font-semibold text-zinc-300 mt-2 mb-1`
    - ul → `list-disc list-inside space-y-1 my-1 text-sm`
    - ol → `list-decimal list-inside space-y-1 my-1 text-sm`
    - li → `text-zinc-200`
    - code (inline) → `font-mono text-xs bg-zinc-800 text-emerald-300 px-1 rounded`
    - pre → `bg-zinc-800 rounded-lg p-3 overflow-x-auto my-2`
    - pre > code → `font-mono text-xs text-emerald-300 leading-relaxed` (no bg, parent pre handles it)
    - blockquote → `border-l-2 border-zinc-600 pl-3 text-zinc-400 italic my-1`
    - hr → `border-zinc-700 my-2`
    - a → `text-blue-400 underline hover:text-blue-300`
    - p → `text-sm text-zinc-50 leading-relaxed my-1`

    NOTE: For the `code` element, check the `node` or parent prop to distinguish inline
    code from block code inside a `pre`. Inline: parent is not `pre`. Block: child of pre.
    In react-markdown v10 the `code` component receives a `children` string; check if it
    is wrapped in a `pre` by using the fact that block code is rendered as `<pre><code>`.
    The simplest pattern: in the `pre` renderer, render `<pre className="..."><code
    className="font-mono text-xs text-emerald-300 leading-relaxed">{children}</code></pre>`
    rather than applying classes in the `code` renderer for the block case.

    Step 3 — Replace raw text render in the assistant bubble:
    In the assistant branch of MessageBubble, replace the existing:
      `{message.content}`
    (currently inside the outer wrapper div that also has `streamingClass`) with:
      `<div className="space-y-1"><ReactMarkdown components={MARKDOWN_COMPONENTS}>{message.content}</ReactMarkdown></div>`

    Move the `streamingClass` (after:content-['▋'] after:animate-pulse after:text-zinc-400)
    from the inner content to the outer bubble wrapper div (the `max-w-[80%] bg-zinc-900 ...`
    div), per UI-SPEC §Component Contracts §1 "Streaming behavior" note. This prevents the
    cursor appearing inside a `<p>` or `<li>`.

    Step 4 — Add required import:
    Add `import ReactMarkdown from 'react-markdown';` at the top of MessageBubble.tsx.
    Add `Copy, Check` from `lucide-react` to the import line (Plan 3 will use them, but
    establishing the import here avoids a merge conflict — only add if not already present).

    Do NOT add `rehype-raw`, `remark-gfm`, or any other plugin prop to ReactMarkdown.
    Do NOT use dangerouslySetInnerHTML anywhere (T-03-14).
    Do NOT change the Message interface, props interface, or post-stream gate logic — those
    are modified in later plans.

    Step 5 — Verify build:
    Run `npm run build` in `frontend/`. Fix any TypeScript errors before marking done.
    Common issue: react-markdown v10 `Components` type — if TS complains about the
    components prop, type MARKDOWN_COMPONENTS as `Record<string, React.ComponentType<React.HTMLAttributes<HTMLElement>>>` or
    import `Components` from `react-markdown` directly.
  </action>

  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>

  <done>
    - frontend/package.json contains "react-markdown": "10.1.0" (or "^10.1.0") in dependencies
    - frontend/components/MessageBubble.tsx imports ReactMarkdown from 'react-markdown'
    - MessageBubble.tsx contains MARKDOWN_COMPONENTS const with at minimum: p, strong, em, ul, ol, li, code, pre, blockquote, hr, a, h1, h2, h3 keys
    - The assistant bubble renders `<ReactMarkdown components={MARKDOWN_COMPONENTS}>{message.content}</ReactMarkdown>` inside a `<div className="space-y-1">`
    - The streamingClass (after:content-['▋']) is on the outer bubble wrapper div, not on the ReactMarkdown wrapper
    - No dangerouslySetInnerHTML anywhere in the file
    - `npm run build` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → ReactMarkdown | Model-generated Markdown string is passed as children to ReactMarkdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ReactMarkdown / rehype-raw | mitigate | No rehype-raw plugin — raw HTML passthrough disabled. JSX mode only. This is the explicit constraint from D-01 and T-03-14. |
| T-04-02 | Tampering | dangerouslySetInnerHTML | mitigate | Confirmed absent: react-markdown v10 JSX mode never calls innerHTML. Code review gate: grep for dangerouslySetInnerHTML must return 0 occurrences. |
</threat_model>

<verification>
1. `grep -r "dangerouslySetInnerHTML" frontend/components/MessageBubble.tsx` returns empty
2. `grep "import ReactMarkdown" frontend/components/MessageBubble.tsx` returns the import line
3. `grep "MARKDOWN_COMPONENTS" frontend/components/MessageBubble.tsx | wc -l` returns >= 2 (definition + usage)
4. `cd frontend && npm run build` exits 0
5. `grep '"react-markdown"' frontend/package.json` returns the version line
</verification>

<success_criteria>
- react-markdown@10.1.0 is installed and listed in package.json
- MARKDOWN_COMPONENTS maps all 13 Markdown element types to their Tailwind classes per UI-SPEC §Typography
- Assistant message content renders via ReactMarkdown with no dangerouslySetInnerHTML (T-03-14 satisfied)
- Streaming cursor (▋) is on the outer bubble wrapper div, not inside a react-markdown-rendered element
- npm run build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish-knobs-markdown-follow-ups/04-01-SUMMARY.md`
</output>

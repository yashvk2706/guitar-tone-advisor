---
phase: 07-persistent-corpus-cloud-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ingest/loader.py
  - requirements.txt
autonomous: true
requirements: [INGEST-10, PERSIST-01]
must_haves:
  truths:
    - "yt-dlp fallback authenticates to YouTube with Chrome cookies and no longer passes the invalid 'nodejs' runtime value"
    - "Installing requirements.txt brings in yt-dlp-ejs so the EJS n-challenge solver is present"
    - "The existing loader test suite still passes after the loader edit"
  artifacts:
    - path: "app/ingest/loader.py"
      provides: "Fixed _load_via_ytdlp() cmd list (cookies-from-browser chrome, no --js-runtimes nodejs)"
      contains: "--cookies-from-browser"
    - path: "requirements.txt"
      provides: "yt-dlp[default] extras for EJS challenge solving"
      contains: "yt-dlp[default]"
  key_links:
    - from: "app/ingest/loader.py::_load_via_ytdlp"
      to: "yt-dlp subprocess"
      via: "cmd list passed to subprocess.run(shell=False)"
      pattern: "--cookies-from-browser.*chrome"
---

<objective>
Fix the YouTube transcript ingestion path so the full corpus pipeline (Workstream A prerequisite) can pull all 13 YouTube videos before the corpus is dumped and restored to Railway.

Purpose: All 13 YouTube videos currently fail with YouTube bot detection. The yt-dlp fallback has two compounding bugs: (1) it passes the invalid runtime value `nodejs` to `--js-runtimes` (correct value is `node`, or omit), and (2) `yt-dlp-ejs` (the EJS n-challenge solver) is not installed because requirements.txt pins bare `yt-dlp`. Both must be fixed before the operator runs the full pipeline in Plan 03.

Output: A corrected `_load_via_ytdlp()` cmd list and a `yt-dlp[default]` requirement line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-persistent-corpus-cloud-deployment/07-CONTEXT.md
@.planning/phases/07-persistent-corpus-cloud-deployment/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the yt-dlp fallback cmd list in _load_via_ytdlp()</name>
  <files>app/ingest/loader.py</files>
  <read_first>
    - app/ingest/loader.py (lines 401-476 — the full _load_via_ytdlp() function; the cmd list is lines 426-435)
    - .planning/phases/07-persistent-corpus-cloud-deployment/07-PATTERNS.md (section "app/ingest/loader.py (MODIFY)" — exact target state)
    - .planning/phases/07-persistent-corpus-cloud-deployment/07-CONTEXT.md (D-12)
  </read_first>
  <action>
    In `_load_via_ytdlp()`, modify the `cmd` list (currently at lines 426-435) per D-12. Make exactly two edits and touch nothing else in the function:
    1. INSERT `"--cookies-from-browser", "chrome",` as the element immediately after `"yt-dlp",`. Add an inline comment noting it bypasses the YouTube IP block with an authenticated session.
    2. REMOVE the existing two-element entry `"--js-runtimes", "nodejs",` (and its trailing comment) entirely. Replace it with a comment explaining the removal: `nodejs` is the wrong runtime value (yt-dlp accepts `node`/`deno`/`bun`/`quickjs`), and `yt-dlp[default]` auto-detects the available runtime via yt-dlp-ejs.
    Leave `--write-auto-subs`, `--sub-lang en`, `--sub-format vtt`, `--skip-download`, the `f"https://www.youtube.com/watch?v={video_id}"` URL, and `-o out_tmpl` in place and in the same order. Do NOT modify the `_VIDEO_ID_RE.match(video_id)` guard, the `subprocess.run(... shell=False ...)` call, the VTT parsing, or the RawDocument construction (T-06-06: shell=False + video_id regex validation must be preserved).
  </action>
  <verify>
    <automated>grep -c "cookies-from-browser" app/ingest/loader.py | grep -qv '^0$' && ! grep -q '"--js-runtimes", "nodejs"' app/ingest/loader.py && venv/bin/python -m pytest tests/test_loader.py -x -q && echo LOADER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/ingest/loader.py` contains the string `--cookies-from-browser` followed by `chrome`
    - `app/ingest/loader.py` does NOT contain `"--js-runtimes", "nodejs"`
    - `app/ingest/loader.py` still contains `shell=False` inside `_load_via_ytdlp()` (T-06-06 mitigation preserved)
    - `app/ingest/loader.py` still contains `_VIDEO_ID_RE.match(video_id)` (T-06-06 mitigation preserved)
    - `venv/bin/python -m pytest tests/test_loader.py -x -q` exits 0
  </acceptance_criteria>
  <done>The cmd list authenticates with Chrome cookies, no longer passes the invalid `nodejs` runtime value, and all loader tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Switch requirements.txt to yt-dlp[default]</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (line 22 — current bare `yt-dlp` entry; surrounding Phase 6 ingestion block is lines 18-23)
    - .planning/phases/07-persistent-corpus-cloud-deployment/07-PATTERNS.md (section "requirements.txt (MODIFY)")
    - .planning/phases/07-persistent-corpus-cloud-deployment/07-CONTEXT.md (D-12)
  </read_first>
  <action>
    Replace the bare `yt-dlp` line (line 22, in the Phase 6 ingestion dependencies block) with `yt-dlp[default]`. Do not add a version pin — keep it unpinned, consistent with the current bare entry (venv currently resolves 2026.3.17). The `[default]` extra installs `yt-dlp-ejs==0.8.0` (plus brotli, mutagen, pycryptodomex), which provides the EJS n-challenge solver scripts. Leave every other line in requirements.txt unchanged, including the LOCKED-version comment header and the commented optional embedding backends at the bottom.
  </action>
  <verify>
    <automated>grep -q '^yt-dlp\[default\]$' requirements.txt && ! grep -q '^yt-dlp$' requirements.txt && echo REQ_OK</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains a line exactly equal to `yt-dlp[default]`
    - `requirements.txt` does NOT contain a line exactly equal to bare `yt-dlp`
    - No other requirement line changed (e.g., `youtube-transcript-api==1.2.4` and `trafilatura==2.0.0` still present)
  </acceptance_criteria>
  <done>requirements.txt pins `yt-dlp[default]`, ensuring yt-dlp-ejs installs for n-challenge solving in both local and container environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → yt-dlp subprocess | `video_id` (read from `raw_data/youtube_ids.txt`) crosses into a subprocess argv |
| local Chrome profile → yt-dlp | Chrome cookies are read and passed to YouTube only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering/Injection | `_load_via_ytdlp()` cmd list | mitigate | Preserve `_VIDEO_ID_RE.match(video_id)` 11-char allowlist guard and `subprocess.run(shell=False)` — the edit only adds/removes static flags, never interpolates untrusted input into a shell |
| T-07-01 | Information Disclosure | `--cookies-from-browser chrome` | accept | yt-dlp reads cookies from the local Chrome profile and passes them to YouTube only; cookies are not logged or persisted beyond the subprocess lifetime (RESEARCH.md Security Domain) |
</threat_model>

<verification>
- `venv/bin/python -m pytest tests/test_loader.py -x -q` exits 0
- `venv/bin/python -m pytest tests/ -q --ignore=tests/test_eval_ragas.py` exits 0 (full regression — no behavior change to other modules)
- Source asserts: `--cookies-from-browser` present, `"--js-runtimes", "nodejs"` absent, `yt-dlp[default]` present in requirements.txt
</verification>

<success_criteria>
- The yt-dlp fallback command authenticates via Chrome cookies and omits the invalid runtime flag
- requirements.txt installs yt-dlp-ejs via the `[default]` extra
- Existing loader and full offline test suites remain green
</success_criteria>

<output>
After completion, create `.planning/phases/07-persistent-corpus-cloud-deployment/07-01-SUMMARY.md`
</output>

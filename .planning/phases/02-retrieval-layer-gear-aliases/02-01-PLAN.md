---
phase: 02-retrieval-layer-gear-aliases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/gear_aliases.json
  - app/retrieval/__init__.py
  - app/retrieval/aliases.py
autonomous: true
requirements:
  - INGEST-07

must_haves:
  truths:
    - "data/gear_aliases.json exists with exactly 14 corpus-grounded alias pairs"
    - "A query containing 'Strat' is expanded to 'Strat Stratocaster' before embedding"
    - "A query containing 'Stratocaster' is expanded to 'Strat Stratocaster' before embedding"
    - "Alias expansion uses word-boundary matching and does not mangle substrings"
    - "expand_query() loads aliases once (lru_cache) and never reads the file on repeated calls"
  artifacts:
    - path: "data/gear_aliases.json"
      provides: "14 corpus-verified bidirectional alias pairs"
      contains: '"aliases"'
    - path: "app/retrieval/__init__.py"
      provides: "package marker"
    - path: "app/retrieval/aliases.py"
      provides: "expand_query() and _load_alias_pairs()"
      exports:
        - expand_query
        - _load_alias_pairs
  key_links:
    - from: "app/retrieval/aliases.py"
      to: "data/gear_aliases.json"
      via: "_ALIASES_PATH resolved from __file__ with three .parent hops"
      pattern: "Path(__file__).resolve().parent.parent.parent"
---

<objective>
Author the corpus-grounded gear alias file and implement the bidirectional query expansion module. This is the foundation that RETR-02 depends on — the retrieval function (Plan 2) calls expand_query() before embedding any user query.

Purpose: Satisfy INGEST-07 (gear_aliases.json + bidirectional expansion before embedding). Every alias pair is traceable to actual forum post text verified by the RESEARCH.md grep scan.
Output: data/gear_aliases.json (14 pairs), app/retrieval/__init__.py (empty package), app/retrieval/aliases.py (loader + expander).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-retrieval-layer-gear-aliases/02-CONTEXT.md
@.planning/phases/02-retrieval-layer-gear-aliases/02-RESEARCH.md
@.planning/phases/02-retrieval-layer-gear-aliases/02-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From app/config.py:
```python
@lru_cache(maxsize=1)
def get_settings() -> Settings: ...
# Settings.embedding_model: str = "text-embedding-3-small"
```

From app/embeddings/base.py:
```python
class Embedder(Protocol):
    def embed_query(self, text: str) -> list[float]: ...
```

From app/db.py:
```python
# Path anchor pattern (two hops from app/db.py to project root):
_INIT_SQL_PATH = Path(__file__).resolve().parent.parent / "scripts" / "init_db.sql"
# aliases.py is one deeper (app/retrieval/aliases.py), so three hops needed:
# Path(__file__).resolve().parent.parent.parent / "data" / "gear_aliases.json"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author data/gear_aliases.json with 14 corpus-verified alias pairs</name>
  <files>data/gear_aliases.json</files>
  <read_first>
    - .planning/phases/02-retrieval-layer-gear-aliases/02-RESEARCH.md (§Alias File: Grounded Corpus Findings — the exact 14 pairs and their source files)
    - .planning/phases/02-retrieval-layer-gear-aliases/02-PATTERNS.md (§data/gear_aliases.json — JSON schema and complete entry list)
    - .planning/phases/02-retrieval-layer-gear-aliases/02-CONTEXT.md (D-01, D-02 — corpus-only scope, no assumed shortforms)
  </read_first>
  <action>
    Create the directory data/ at the project root (adjacent to app/, scripts/, tests/). Create data/gear_aliases.json with a top-level "aliases" key containing exactly 14 objects, each with "shortform" and "canonical" string fields.

    The 14 entries in order (all verified against raw_data/forum_posts/*.txt per RESEARCH.md):
    1. shortform: "Strat", canonical: "Stratocaster"
    2. shortform: "Tele", canonical: "Telecaster"
    3. shortform: "EVH", canonical: "Eddie Van Halen"
    4. shortform: "SLO", canonical: "Soldano SLO-100"
    5. shortform: "5150", canonical: "Peavey 5150"
    6. shortform: "6505", canonical: "Peavey 6505"
    7. shortform: "AX8", canonical: "Fractal AX8"
    8. shortform: "SD1", canonical: "Boss SD-1"
    9. shortform: "GE-7", canonical: "Boss GE-7"
    10. shortform: "HM-2", canonical: "Boss HM-2"
    11. shortform: "Dual Rec", canonical: "Mesa Dual Rectifier"
    12. shortform: "Maxon 808", canonical: "Maxon OD808"
    13. shortform: "SRV", canonical: "Stevie Ray Vaughan"
    14. shortform: "SSS", canonical: "Dumble Steel String Slinger"

    Do NOT add TS9, JCM800, BD-2, or Klon — RESEARCH.md §Intentionally NOT in the file explains these are excluded because the shortform tokens themselves do not appear in the corpus.

    Use 2-space indentation. The file must be valid JSON (no trailing commas).
  </action>
  <verify>
    <automated>python -c "import json; d = json.loads(open('data/gear_aliases.json').read()); assert len(d['aliases']) == 14; assert all('shortform' in e and 'canonical' in e for e in d['aliases']); print('OK', len(d['aliases']), 'pairs')"</automated>
  </verify>
  <done>
    data/gear_aliases.json exists at project root level, parses as valid JSON, contains "aliases" key with exactly 14 entries each having "shortform" and "canonical" string fields. All 14 pairs match the RESEARCH.md verified list. No TS9, JCM800, BD-2, or Klon entries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/retrieval/__init__.py and app/retrieval/aliases.py</name>
  <files>app/retrieval/__init__.py, app/retrieval/aliases.py</files>
  <read_first>
    - app/config.py (lru_cache pattern — lines 41–45; Settings fields)
    - app/db.py (Path(__file__).resolve().parent.parent path anchor — line 24)
    - app/embeddings/factory.py (module structure and import conventions)
    - .planning/phases/02-retrieval-layer-gear-aliases/02-PATTERNS.md (§app/retrieval/aliases.py — full pattern with imports, path anchor, lru_cache loader, expand_query signature)
    - .planning/phases/02-retrieval-layer-gear-aliases/02-CONTEXT.md (D-03, D-04, D-05 — exact re.sub pattern, count=1, IGNORECASE)
    - .planning/phases/02-retrieval-layer-gear-aliases/02-RESEARCH.md (Pattern 4: Word-Boundary Alias Expansion — edge cases including multi-word canonical and Pitfall 3 double-expansion)
  </read_first>
  <behavior>
    - expand_query("Strat neck pickup clean tone", [("Strat", "Stratocaster")]) returns "Strat Stratocaster neck pickup clean tone"
    - expand_query("Stratocaster neck pickup clean tone", [("Strat", "Stratocaster")]) returns "Strat Stratocaster neck pickup clean tone"
    - expand_query("strat tone", [("Strat", "Stratocaster")]) returns "Strat Stratocaster tone" (case-insensitive)
    - expand_query("Strat Strat tone", [("Strat", "Stratocaster")]) returns "Strat Stratocaster Strat tone" (count=1, only first occurrence expanded)
    - expand_query("no match here", [("Strat", "Stratocaster")]) returns "no match here" unchanged
    - expand_query("Stratocaster tone", []) returns "Stratocaster tone" unchanged (empty pairs)
    - _load_alias_pairs() returns list of 14 (shortform, canonical) tuples when data/gear_aliases.json exists
  </behavior>
  <action>
    Create app/retrieval/__init__.py as an empty package marker (single blank line; no imports, no re-exports). Mirror app/embeddings/__init__.py exactly.

    Create app/retrieval/aliases.py with the following structure (per D-03, D-05):

    First line: from __future__ import annotations
    Imports (stdlib only): json, re, lru_cache from functools, Path from pathlib. No app imports needed.

    Module-level path constant (three .parent hops because aliases.py is at app/retrieval/aliases.py):
    _ALIASES_PATH = Path(__file__).resolve().parent.parent.parent / "data" / "gear_aliases.json"

    _load_alias_pairs() decorated with @lru_cache(maxsize=1). Reads _ALIASES_PATH via Path.read_text(encoding="utf-8"), json.loads, then returns [(entry["shortform"], entry["canonical"]) for entry in data["aliases"]].

    expand_query(query: str, alias_pairs: list[tuple[str, str]] | None = None) -> str:
    - If alias_pairs is None, call _load_alias_pairs() to get pairs (allows test injection without monkeypatching)
    - Iterate over (shortform, canonical) pairs
    - replacement = f"{shortform} {canonical}"
    - Apply re.sub(rf"\b{re.escape(shortform)}\b", replacement, result, count=1, flags=re.IGNORECASE)
    - Apply re.sub(rf"\b{re.escape(canonical)}\b", replacement, result, count=1, flags=re.IGNORECASE)
    - Return final result string

    Per RESEARCH.md Pitfall 6: for multi-word canonicals like "Dumble Steel String Slinger", normalize the query first with " ".join(query.split()) to collapse whitespace before expansion. Apply this normalization at the start of expand_query before the loop.

    Do NOT import openai, openai_embedder, or any OpenAI SDK symbol. Do NOT import psycopg. This module is pure stdlib.
  </action>
  <verify>
    <automated>pytest tests/test_retrieval.py::test_aliases_json_loads tests/test_retrieval.py::test_expand_shortform tests/test_retrieval.py::test_expand_canonical -x -q</automated>
  </verify>
  <done>
    app/retrieval/__init__.py exists (empty package marker). app/retrieval/aliases.py implements _load_alias_pairs() with @lru_cache(maxsize=1) and expand_query() with re.sub word-boundary matching (count=1, IGNORECASE). Tests test_aliases_json_loads, test_expand_shortform, and test_expand_canonical all pass. No openai or psycopg imports present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → aliases.py | gear_aliases.json is read from disk at module load; path is anchored to __file__, not user input |
| user query → expand_query() | Raw user text passes through re.sub transforms (no eval, no exec) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | _ALIASES_PATH construction | mitigate | Path resolved from `Path(__file__).resolve().parent.parent.parent` — not from user input; no path traversal vector |
| T-01-02 | Tampering | expand_query re.sub | accept | re.sub with re.escape(alias) is safe; no eval/exec anywhere in the alias expansion path; ASVS V5 satisfied |
| T-01-03 | Denial of Service | _load_alias_pairs on missing file | accept | FileNotFoundError surfaces at first expand_query call; acceptable for local single-user tool where operator controls filesystem |
</threat_model>

<verification>
After both tasks complete:

```bash
# JSON structure valid, 14 pairs
python -c "import json; d=json.loads(open('data/gear_aliases.json').read()); assert len(d['aliases'])==14; print('alias file OK')"

# Alias expansion unit tests
pytest tests/test_retrieval.py::test_aliases_json_loads tests/test_retrieval.py::test_expand_shortform tests/test_retrieval.py::test_expand_canonical -x -q

# No openai import in retrieval package
python -c "
import re, pathlib
pattern = re.compile(r'^(from openai\b|import openai\b)', re.MULTILINE)
for f in pathlib.Path('app/retrieval').rglob('*.py'):
    text = f.read_text()
    assert not pattern.search(text), f'openai import found in {f}'
print('no direct openai imports OK')
"
```
</verification>

<success_criteria>
- data/gear_aliases.json contains exactly 14 entries matching the RESEARCH.md corpus-verified list
- app/retrieval/__init__.py exists as an empty package marker
- app/retrieval/aliases.py exports _load_alias_pairs (lru_cache) and expand_query
- expand_query("Strat clean tone", [("Strat","Stratocaster")]) returns "Strat Stratocaster clean tone"
- expand_query("Stratocaster clean tone", [("Strat","Stratocaster")]) returns "Strat Stratocaster clean tone"
- No openai or psycopg imports anywhere in app/retrieval/
- test_aliases_json_loads, test_expand_shortform, test_expand_canonical all pass
</success_criteria>

<output>
After completion, create .planning/phases/02-retrieval-layer-gear-aliases/02-01-SUMMARY.md
</output>

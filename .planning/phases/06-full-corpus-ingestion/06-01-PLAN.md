---
phase: "06-full-corpus-ingestion"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ingest/writer.py
  - app/ingest/loader.py
  - tests/test_writer.py
autonomous: true
requirements:
  - INGEST-08
  - INGEST-09
  - INGEST-10

must_haves:
  truths:
    - "upsert_chunks() accepts source_type as a required parameter and uses it for every chunk row"
    - "RawDocument has a metadata field for threading per-segment data (YouTube start times) into the chunker"
    - "No chunk is ever written with source_type hard-coded to 'forum' regardless of the actual document type"
    - "All existing test_writer.py tests still pass after the signature change"
  artifacts:
    - path: "app/ingest/writer.py"
      provides: "Fixed upsert_chunks with source_type parameter"
      contains: "def upsert_chunks"
    - path: "app/ingest/loader.py"
      provides: "RawDocument with metadata field"
      contains: "metadata: dict"
    - path: "tests/test_writer.py"
      provides: "Test verifying source_type is stored correctly"
      contains: "test_upsert_chunks_uses_source_type"
  key_links:
    - from: "app/ingest/pipeline.py"
      to: "app/ingest/writer.py::upsert_chunks"
      via: "source_type keyword argument"
      pattern: "upsert_chunks.*source_type"
---

<objective>
Fix the writer bug that would corrupt all Phase 6 chunk rows, then extend RawDocument to carry per-segment metadata needed by the YouTube chunker. This is the mandatory Wave 1 unblock — no new source type can write correct chunks until upsert_chunks() accepts the real source_type from the calling pipeline.

Purpose: Without this fix every PDF, article, and YouTube chunk would be tagged source_type='forum' in the chunks table, making them indistinguishable from forum posts in retrieval metadata and breaking CITE-02 source-type label rendering.
Output: Fixed writer.py (source_type parameter), extended loader.py (metadata field), updated pipeline.py call site, new writer test verifying the fix.
</objective>

<execution_context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-RESEARCH.md
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-CONTEXT.md
</execution_context>

<context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/ROADMAP.md
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/REQUIREMENTS.md

<interfaces>
<!-- Current upsert_chunks signature (BROKEN — uses _PHASE_1_SOURCE_TYPE = "forum") -->
From app/ingest/writer.py (lines 142-210):

_PHASE_1_SOURCE_TYPE = "forum"   # hardcoded — REMOVE THIS

def upsert_chunks(
    conn: psycopg.Connection,
    document_id: str,
    chunks: Sequence[Chunk],
    vectors: Sequence[Sequence[float]],
    embedding_model: str,
) -> int:
    ...
    params = [(str(uuid.uuid5(_CHUNK_NS, c.content_hash)), document_id,
               _PHASE_1_SOURCE_TYPE, ...)]  # WRONG

<!-- Current RawDocument dataclass (no metadata field) -->
From app/ingest/loader.py:

@dataclass(frozen=True)
class RawDocument:
    source_type: str
    source_id: str
    title: str | None
    text: str
    content_hash: str

<!-- Current pipeline call site that must also be updated -->
From app/ingest/pipeline.py (line 123-129):

upsert_chunks(
    conn,
    doc_id,
    to_embed,
    result.vectors,
    embedder.model,
    # MISSING source_type argument — must add raw_doc.source_type
)

<!-- Current test_writer.py call (will break after signature change) -->
From tests/test_writer.py (tests 5, 7, 8, 11):

upsert_chunks(db_conn, doc_id, chunks, vectors, "text-embedding-3-small")
# → becomes: upsert_chunks(db_conn, doc_id, chunks, vectors, "text-embedding-3-small", source_type="forum")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix upsert_chunks signature + extend RawDocument</name>
  <files>app/ingest/writer.py, app/ingest/loader.py, app/ingest/pipeline.py</files>
  <read_first>
    app/ingest/writer.py
    app/ingest/loader.py
    app/ingest/pipeline.py
    tests/test_writer.py
  </read_first>
  <behavior>
    - upsert_chunks(conn, document_id, chunks, vectors, embedding_model, source_type) accepts source_type as a required keyword argument
    - The _PHASE_1_SOURCE_TYPE = "forum" module-level constant is removed entirely from writer.py
    - The params tuple in upsert_chunks uses the source_type parameter, not the removed constant
    - RawDocument gains a metadata field: metadata: dict[str, Any] = dataclasses.field(default_factory=dict) — frozen dataclass requires field() for mutable defaults; also requires importing dataclasses and Any
    - pipeline.py call site updated to pass source_type=raw_doc.source_type as a keyword argument
    - All existing test_writer.py calls to upsert_chunks must be updated to pass source_type="forum" explicitly so existing tests continue to pass
  </behavior>
  <action>
    In app/ingest/writer.py: Remove the module-level constant _PHASE_1_SOURCE_TYPE = "forum". Add source_type: str as the sixth parameter to upsert_chunks() (after embedding_model). In the params list comprehension, replace _PHASE_1_SOURCE_TYPE with the source_type parameter. Update the function docstring to document the new parameter.

    In app/ingest/loader.py: Add imports for dataclasses (the dataclasses module, not just @dataclass) and Any from typing. Add metadata: dict[str, Any] = dataclasses.field(default_factory=dict) as the last field on RawDocument. Since RawDocument is frozen=True, mutable defaults require dataclasses.field(default_factory=dict) — do NOT assign metadata={} directly (frozen dataclass limitation). The load_forum_posts() function does NOT need to pass metadata= explicitly; omitting it uses the default empty dict.

    In app/ingest/pipeline.py: Update the upsert_chunks() call to add source_type=raw_doc.source_type as a keyword argument.

    In tests/test_writer.py: Update every call to upsert_chunks() to pass source_type="forum" explicitly (there are 4 call sites: tests 5, 7, 8, 11). Do not change any assertion logic.
  </action>
  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor && venv/bin/python -m pytest tests/test_writer.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_no_fstring_sql_in_writer passes (no f-string SQL); all DB-gated tests skip gracefully when Postgres is unavailable; app/ingest/writer.py contains no _PHASE_1_SOURCE_TYPE; upsert_chunks signature has source_type parameter; RawDocument has metadata field; pipeline.py call passes source_type=raw_doc.source_type
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_upsert_chunks_uses_source_type to test_writer.py</name>
  <files>tests/test_writer.py</files>
  <read_first>
    tests/test_writer.py
    app/ingest/writer.py
  </read_first>
  <behavior>
    - test_upsert_chunks_uses_source_type(db_conn) inserts a chunk with source_type="pdf_manual" and reads it back from the chunks table
    - The stored source_type column value equals "pdf_manual" (not "forum")
    - The test uses the _make_raw_doc helper with source_type="pdf_manual" for the document upsert, then upsert_chunks with source_type="pdf_manual"
    - The test asserts: SELECT source_type FROM chunks WHERE document_id = ... returns "pdf_manual"
  </behavior>
  <action>
    Add the test function test_upsert_chunks_uses_source_type(db_conn) to tests/test_writer.py. The test:
    1. Creates a RawDocument with source_type="pdf_manual" using _make_raw_doc() — must override source_type (the factory defaults to "forum", but upsert_document uses raw_doc.source_type for the documents table, which accepts "pdf_manual" per the CHECK constraint in init_db.sql)
    2. Calls upsert_document(db_conn, raw) to get doc_id
    3. Calls upsert_chunks(db_conn, doc_id, [_make_chunk(0)], [_fake_vector()], "text-embedding-3-small", source_type="pdf_manual")
    4. Commits
    5. Runs SELECT source_type FROM chunks WHERE document_id = doc_id AND chunk_index = 0
    6. Asserts row[0] == "pdf_manual"

    Also add test_upsert_chunks_source_type_not_hardcoded() (static, no DB) that reads writer.py text and asserts "_PHASE_1_SOURCE_TYPE" does NOT appear anywhere in the file. This runs without Postgres.
  </action>
  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor && venv/bin/python -m pytest tests/test_writer.py::test_upsert_chunks_source_type_not_hardcoded -x -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_upsert_chunks_source_type_not_hardcoded passes without Postgres (static scan); test_upsert_chunks_uses_source_type is present and will pass when Postgres is available; grep -c "_PHASE_1_SOURCE_TYPE" app/ingest/writer.py returns 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer.py params | source_type value flows from raw_doc.source_type into DB INSERT — caller controls the value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | upsert_chunks source_type param | accept | Values are constrained by the DB CHECK constraint source_type IN ('forum','pdf_manual','web_article','youtube'); any invalid value raises psycopg CheckViolation before storage |
| T-06-02 | Tampering | F-string SQL in writer | mitigate | test_no_fstring_sql_in_writer static scan enforces %s placeholders; no new SQL in this plan |
</threat_model>

<verification>
Static (no Postgres needed):
  pytest tests/test_writer.py::test_no_fstring_sql_in_writer tests/test_writer.py::test_upsert_chunks_source_type_not_hardcoded -v

Grep gate (must return 0):
  grep -v '^#' app/ingest/writer.py | grep -c '_PHASE_1_SOURCE_TYPE'

Signature check:
  grep -n 'def upsert_chunks' app/ingest/writer.py
  # Must show: def upsert_chunks(conn, document_id, chunks, vectors, embedding_model, source_type

RawDocument metadata field:
  grep -n 'metadata' app/ingest/loader.py
  # Must show the field definition with dataclasses.field(default_factory=dict)

Pipeline call site:
  grep -n 'source_type' app/ingest/pipeline.py
  # Must show source_type=raw_doc.source_type in the upsert_chunks call
</verification>

<success_criteria>
- app/ingest/writer.py: upsert_chunks() has source_type: str parameter; _PHASE_1_SOURCE_TYPE constant is gone; source_type parameter used in params tuple
- app/ingest/loader.py: RawDocument has metadata: dict[str, Any] = dataclasses.field(default_factory=dict) as last field
- app/ingest/pipeline.py: upsert_chunks call passes source_type=raw_doc.source_type
- tests/test_writer.py: all existing upsert_chunks call sites pass source_type="forum"; test_upsert_chunks_source_type_not_hardcoded passes; test_upsert_chunks_uses_source_type exists
- pytest tests/test_writer.py::test_no_fstring_sql_in_writer exits 0
- pytest tests/test_writer.py -x exits 0 (or skips DB tests gracefully)
</success_criteria>

<output>
After completion, create /Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-01-SUMMARY.md
</output>

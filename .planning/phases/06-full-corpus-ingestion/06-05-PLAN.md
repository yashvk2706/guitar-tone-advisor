---
phase: "06-full-corpus-ingestion"
plan: "05"
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - app/ingest/pipeline.py
  - requirements.txt
  - tests/test_pipeline.py
autonomous: false
requirements:
  - INGEST-08
  - INGEST-09
  - INGEST-10
  - EVAL-05

must_haves:
  truths:
    - "python -m app.ingest.pipeline ingests all 4 source types: forum, pdf_manual, youtube, web_article"
    - "End-of-run summary prints 'PDFs: N of M ingested (K skipped)', 'Transcripts: N of M', 'Articles: N of M'"
    - "Chunk count grows from 21 to >= 200 after full pipeline run"
    - "Re-running the pipeline on unchanged input embeds zero new chunks (content-hash dedup holds for all source types)"
    - "python -m app.eval.retrieval --held-out shows recall@8 >= 1.0 and MRR >= 0.9"
    - "python -m app.eval.ragas shows mean faithfulness >= 0.5"
    - "requirements.txt includes pymupdf4llm, pypdf, youtube-transcript-api, yt-dlp, trafilatura"
  artifacts:
    - path: "app/ingest/pipeline.py"
      provides: "Extended pipeline orchestrating all 4 source types"
      contains: "load_pdf_manuals\|load_youtube_transcripts\|load_web_articles"
    - path: "requirements.txt"
      provides: "Pinned new dependencies"
      contains: "pymupdf4llm\|youtube-transcript-api\|trafilatura"
    - path: "tests/test_pipeline.py"
      provides: "Static test verifying all 4 loader imports + no direct openai import"
      contains: "test_pipeline_imports_all_loaders"
  key_links:
    - from: "app/ingest/pipeline.py::main"
      to: "load_pdf_manuals, load_youtube_transcripts, load_web_articles"
      via: "sequential calls after load_forum_posts"
      pattern: "load_pdf_manuals\|load_youtube_transcripts\|load_web_articles"
    - from: "app/ingest/pipeline.py"
      to: "app/ingest/writer.py::upsert_chunks"
      via: "source_type=raw_doc.source_type keyword argument"
      pattern: "upsert_chunks.*source_type"
---

<objective>
Wire all four loaders into the pipeline, install new dependencies, print per-source end-of-run summaries, and verify eval scores after the full corpus run. This is the final plan in Phase 6 — it culminates in a human checkpoint that confirms recall@8 >= 1.0, MRR >= 0.9, and faithfulness >= 0.5 against the held-out golden eval set.

Purpose: The pipeline orchestration is what makes the three new source types actually ingested. Without this plan, the loaders and chunkers exist but nothing calls them. The eval checkpoint closes the EVAL-05 requirement.
Output: Extended pipeline.py; requirements.txt updated; static pipeline import test; human eval verification checkpoint.
</objective>

<execution_context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-RESEARCH.md
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-CONTEXT.md
</execution_context>

<context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/ROADMAP.md

<interfaces>
<!-- Current pipeline.py main() structure (to extend) -->
From app/ingest/pipeline.py:

def main(argv=None) -> int:
    parser = _build_parser()
    args = parser.parse_args(argv)
    embedder = get_embedder()
    conn = get_conn()
    # start_run, optional truncate_all
    raw_docs = load_forum_posts(Path(args.forum_dir))
    # for raw_doc in raw_docs: upsert_document → chunk_document → embed → upsert_chunks(source_type=raw_doc.source_type)
    # complete_run, print summary
    # on exception: rollback, fail_run via fresh conn

<!-- New loaders to wire in (implemented in Plans 02, 03, 04) -->
from app.ingest.loader import (
    load_forum_posts,
    load_pdf_manuals,        # Plan 02
    load_youtube_transcripts, # Plan 03
    load_web_articles,        # Plan 04
)

<!-- Per-source skip tracking needed for end-of-run summary (D-07) -->
# Track per-source: (ingested_count, skipped_count, total_count)
# Loaders that skip internally (YouTube, articles) set total_count from the input list
# Pipeline-level skip = chunks_to_embed returns to_skip (dedup) — that is INGEST-06 skipping
# Source-level skip = loader logged WARNING and excluded item — tracked separately

# End-of-run summary format (D-07):
# "PDFs: N of M ingested (K skipped)"
# "Transcripts: N of M ingested (K skipped)"
# "Articles: N of M ingested (K skipped)"
# Printed after all source types complete, before process exit

<!-- New CLI arguments needed -->
--manuals-dir  default="raw_data/manuals"
--youtube-ids  default="raw_data/youtube_ids.txt"
--article-urls default="raw_data/article_urls.txt"

<!-- requirements.txt — must add (from RESEARCH.md Environment Availability) -->
pymupdf4llm==0.0.25
pypdf==5.9.0
youtube-transcript-api==1.2.4
yt-dlp
trafilatura==2.0.0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend pipeline.py + update requirements.txt</name>
  <files>app/ingest/pipeline.py, requirements.txt, tests/test_pipeline.py</files>
  <read_first>
    app/ingest/pipeline.py
    requirements.txt
    tests/test_pipeline.py
  </read_first>
  <behavior>
    - requirements.txt: add pymupdf4llm==0.0.25, pypdf==5.9.0, youtube-transcript-api==1.2.4, yt-dlp, trafilatura==2.0.0 on separate lines
    - pipeline.py imports: add load_pdf_manuals, load_youtube_transcripts, load_web_articles to the from app.ingest.loader import block
    - _build_parser(): add three new optional arguments: --manuals-dir (default "raw_data/manuals"), --youtube-ids (default "raw_data/youtube_ids.txt"), --article-urls (default "raw_data/article_urls.txt")
    - main(): after the forum loop completes, run three additional source-type loops in sequence: PDF manuals, YouTube transcripts, web articles. Each loop uses the same pattern: load → upsert_document → chunk_document → chunks_to_embed → embed → upsert_chunks(source_type=raw_doc.source_type) → conn.commit() per document
    - Per-source counters: track (total_attempted, total_ingested, total_skipped) for each of the 3 new source types. Loaders return only successfully-loaded docs; to count total attempted, pass the raw_data file path and count lines/files before calling the loader, or infer from the loaded list + any gaps
    - End-of-run summary (D-07): after all loops, print to stdout: "PDFs: {n_pdf_ingested} of {n_pdf_total} ingested ({n_pdf_skipped} skipped)", "Transcripts: {n_yt_ingested} of {n_yt_total} ingested ({n_yt_skipped} skipped)", "Articles: {n_art_ingested} of {n_art_total} ingested ({n_art_skipped} skipped)". These lines appear AFTER the existing "OK: N chunks inserted" line.
    - complete_run() counters aggregate ALL source types (forum + pdf + youtube + articles)
    - test_pipeline_imports_all_loaders (static, no DB): reads pipeline.py text and asserts "load_pdf_manuals", "load_youtube_transcripts", "load_web_articles" all appear
    - test_pipeline_no_openai_import (already exists as test_uses_only_embedder_protocol — verify it still passes)
    - test_help_includes_new_args (static): run main(["--help"]) and assert "--manuals-dir", "--youtube-ids", "--article-urls" appear in help output
  </behavior>
  <action>
    In requirements.txt: Read the current file first (pip freeze or manual list). Append the 5 new dependencies on new lines. Verify no duplicate entries.

    In app/ingest/pipeline.py: Update the import block to include the 3 new loader functions. Update _build_parser() to add --manuals-dir, --youtube-ids, --article-urls arguments. Update main() to run 3 additional loops after the forum loop.

    For per-source totals: count total PDF files via len(list(Path(args.manuals_dir).glob("*.pdf"))) before calling load_pdf_manuals(); count total YouTube IDs from args.youtube_ids file using the same _parse_youtube_ids logic (or just len of parsed list before passing to loader — simplest is to get the count from the loader's input, not output); count total article URLs from args.article_urls file. The difference between total_attempted and len(loaded_docs) gives skipped count.

    Print end-of-run summaries via print() (not logger.info) so they appear in stdout regardless of log level — consistent with the existing "OK: N chunks inserted" print statement.

    In tests/test_pipeline.py: Add test_pipeline_imports_all_loaders() as a static test (always runs, no DB). Add test_help_includes_new_args() using capsys to capture --help output and assert new arg names appear.
  </action>
  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor && venv/bin/python -m pytest tests/test_pipeline.py::test_pipeline_imports_all_loaders tests/test_pipeline.py::test_help_includes_new_args tests/test_pipeline.py::test_uses_only_embedder_protocol tests/test_pipeline.py::test_help_works -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_pipeline_imports_all_loaders passes (all 3 new loader names in pipeline.py); test_help_includes_new_args passes (--manuals-dir, --youtube-ids, --article-urls in --help output); test_uses_only_embedder_protocol still passes; requirements.txt contains pymupdf4llm, pypdf, youtube-transcript-api, yt-dlp, trafilatura; pytest 4 static pipeline tests -x exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full pipeline run + eval score verification (EVAL-05)</name>
  <what-built>
    Pipeline extended to ingest all 4 source types. Dependencies installed.
    Install the new dependencies before running:

      cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor
      source venv/bin/activate
      pip install pymupdf4llm==0.0.25 pypdf==5.9.0 youtube-transcript-api==1.2.4 yt-dlp trafilatura==2.0.0

    Run the full pipeline:

      python -m app.ingest.pipeline --full-rebuild

    Expect output lines like:
      "PDFs: 15 of 15 ingested (0 skipped)"
      "Transcripts: N of 13 ingested (K skipped)"
      "Articles: N of 10 ingested (K skipped)"
      "OK: N chunks inserted, ..."

    Then run eval:

      python -m app.eval.retrieval --held-out
      python -m app.eval.ragas
  </what-built>
  <how-to-verify>
    1. After pip install: run pytest tests/ -x --ignore=tests/test_pipeline.py (skip live pipeline tests that need OPENAI_API_KEY) — all offline tests must pass

    2. Run python -m app.ingest.pipeline --full-rebuild with OPENAI_API_KEY and DATABASE_URL set
       - Confirm no unhandled exceptions
       - Confirm output includes all 3 end-of-run summary lines ("PDFs: ...", "Transcripts: ...", "Articles: ...")
       - Confirm chunk count > 21: SELECT COUNT(*) FROM chunks; — expect >= 200

    3. Run python -m app.eval.retrieval --held-out
       - Confirm recall@8 >= 1.0 and MRR >= 0.9 are printed

    4. Run python -m app.eval.ragas
       - Confirm mean faithfulness >= 0.5 is printed

    5. Re-run python -m app.ingest.pipeline (no --full-rebuild)
       - Confirm "OK: 0 chunks inserted, N skipped" — idempotency holds for all 4 source types
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 verification steps pass.
    Describe any failures (eval scores below threshold, pipeline errors, chunk count < 200).
    If eval scores are below threshold, note which ones failed and what was observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline.py → loaders | All 4 loader functions are pure Python; embedder is the only external API call |
| requirements.txt | Pinned versions prevent supply-chain drift; yt-dlp left unpinned (actively maintained, pin adds maintenance burden) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | New CLI args (--manuals-dir etc.) path injection | mitigate | Each path argument is passed to Path().resolve() + glob inside the loader functions (same T-02-01 pattern used for --forum-dir in Phase 1) |
| T-06-14 | Tampering | F-string SQL in extended pipeline | mitigate | test_no_fstring_sql_in_writer covers writer.py; pipeline.py has no SQL; static test_uses_only_embedder_protocol enforces no direct openai import |
| T-06-15 | Denial of Service | Full pipeline with 15 PDFs + 13 YT + 10 articles | accept | Single-user local tool; pipeline is a one-time offline operation; long runtime is expected and acceptable |
</threat_model>

<verification>
Static tests (no network, no DB, no OPENAI_API_KEY):
  pytest tests/test_pipeline.py::test_pipeline_imports_all_loaders \
         tests/test_pipeline.py::test_help_includes_new_args \
         tests/test_pipeline.py::test_uses_only_embedder_protocol \
         tests/test_pipeline.py::test_help_works -v

Full offline test suite (before live run):
  pytest tests/ -x -v --ignore=tests/test_pipeline.py 2>&1 | tail -10
  # All offline tests must pass

Post-ingest DB check:
  psql $DATABASE_URL -c "SELECT source_type, COUNT(*) FROM chunks GROUP BY source_type ORDER BY source_type;"
  # Must show rows for forum, pdf_manual, youtube, web_article

Idempotency check:
  python -m app.ingest.pipeline 2>&1 | grep "0 chunks inserted"
  # Second run must insert 0 chunks

Eval gate:
  python -m app.eval.retrieval --held-out 2>&1 | grep -E "recall@8|MRR"
  # recall@8 >= 1.0, MRR >= 0.9

  python -m app.eval.ragas 2>&1 | grep "faithfulness"
  # faithfulness >= 0.5
</verification>

<success_criteria>
- requirements.txt contains all 5 new dependencies (pymupdf4llm, pypdf, youtube-transcript-api, yt-dlp, trafilatura)
- pipeline.py imports and calls load_pdf_manuals, load_youtube_transcripts, load_web_articles
- --help output shows --manuals-dir, --youtube-ids, --article-urls
- Full pipeline run produces >= 200 chunks across all 4 source_types
- SELECT DISTINCT source_type FROM chunks returns {forum, pdf_manual, youtube, web_article}
- Re-run of pipeline with same input inserts 0 new chunks (INGEST-06 dedup holds)
- python -m app.eval.retrieval --held-out: recall@8 >= 1.0 and MRR >= 0.9
- python -m app.eval.ragas: mean faithfulness >= 0.5
- Human checkpoint: "approved" received
</success_criteria>

<output>
After completion, create /Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-05-SUMMARY.md
</output>

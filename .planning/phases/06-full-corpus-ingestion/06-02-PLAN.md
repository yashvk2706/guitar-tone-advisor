---
phase: "06-full-corpus-ingestion"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - app/ingest/loader.py
  - app/ingest/chunker.py
  - tests/test_loader.py
  - tests/test_chunker.py
autonomous: true
requirements:
  - INGEST-08

must_haves:
  truths:
    - "load_pdf_manuals() returns one RawDocument per PDF in raw_data/manuals/ with source_type='pdf_manual' and source_id as full absolute path"
    - "chunk_pdf() receives raw_doc.source_id as a full absolute path (set by loader) and passes it directly to pymupdf4llm.to_markdown() — no path reconstruction needed"
    - "chunk_pdf() never splits inside a GFM table block (| lines form atomic units)"
    - "PDF chunker skips page index 0 (cover page) and any page where >50% of lines are short (ToC heuristic)"
    - "Each PDF chunk's metadata carries section_heading (str) and page_number (int, 1-based)"
    - "pymupdf4llm is primary extractor; pypdf is fallback when pymupdf4llm raises"
    - "chunk_document() dispatch now routes 'pdf_manual' to chunk_pdf() without raising NotImplementedError"
  artifacts:
    - path: "app/ingest/loader.py"
      provides: "load_pdf_manuals() function"
      contains: "def load_pdf_manuals"
    - path: "app/ingest/chunker.py"
      provides: "chunk_pdf() function + updated dispatch"
      contains: "def chunk_pdf"
    - path: "tests/test_loader.py"
      provides: "PDF loader unit tests"
      contains: "test_load_pdf_manuals_source_type"
    - path: "tests/test_chunker.py"
      provides: "PDF chunker unit tests including table-split guard"
      contains: "test_pdf_chunker_no_table_split"
  key_links:
    - from: "app/ingest/chunker.py::chunk_document"
      to: "app/ingest/chunker.py::chunk_pdf"
      via: "elif raw_doc.source_type == 'pdf_manual'"
      pattern: "pdf_manual.*chunk_pdf"
    - from: "app/ingest/chunker.py::chunk_pdf"
      to: "pymupdf4llm.to_markdown"
      via: "primary extraction path"
      pattern: "pymupdf4llm.to_markdown"
---

<objective>
Implement the PDF manual loader and chunker. This plan delivers INGEST-08 fully: load_pdf_manuals() reads all 15 PDFs from raw_data/manuals/, chunk_pdf() uses pymupdf4llm for section-aware heading detection with a pypdf fallback, and the table-split hard constraint (CLAUDE.md + D-06) is enforced by treating any run of |-prefixed lines as an atomic unit.

Purpose: PDF manuals (Marshall JCM800, Fender Deluxe Reverb, Mesa Boogie Mark V, etc.) are the highest-signal corpus source for technical amp settings. Without them, eval recall@8 against the held-out set is limited.
Output: load_pdf_manuals() + chunk_pdf() functions; updated chunk_document() dispatch; unit tests for both.
</objective>

<execution_context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-RESEARCH.md
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-CONTEXT.md
</execution_context>

<context>
@/Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/ROADMAP.md

<interfaces>
<!-- From app/ingest/loader.py (after Plan 01 — RawDocument now has metadata field) -->
@dataclass(frozen=True)
class RawDocument:
    source_type: str
    source_id: str
    title: str | None
    text: str
    content_hash: str
    metadata: dict[str, Any] = dataclasses.field(default_factory=dict)

def load_forum_posts(directory: Path) -> list[RawDocument]:
    # Pattern to follow: Path.resolve() + sorted glob + NFKC normalize + sha256 + title

<!-- From app/ingest/chunker.py -->
_ENCODING = tiktoken.get_encoding("cl100k_base")
MAX_TOKENS = 500
MIN_PARAGRAPH_WORDS = 40

@dataclass(frozen=True)
class Chunk:
    chunk_index: int
    text: str
    token_count: int
    content_hash: str
    metadata: dict[str, Any]

def chunk_document(raw_doc: RawDocument) -> list[Chunk]:
    if raw_doc.source_type == "forum":
        return chunk_forum(raw_doc)
    raise NotImplementedError(...)   # ← add elif "pdf_manual" branch before this

def _finalize_chunk(blocks: list[str], chunk_index: int, source_id: str) -> Chunk:
    # Reuse in chunk_pdf for text assembly + hash computation

<!-- pymupdf4llm API (verified) -->
pages: list[dict] = pymupdf4llm.to_markdown(str(pdf_path), page_chunks=True, pages=[...])
# Each dict: page["metadata"]["page_number"] (1-based int), page["text"] (GFM markdown str)
# Tables appear as runs of | col | col | lines in page["text"]

<!-- pypdf fallback API (verified) -->
from pypdf import PdfReader
reader = PdfReader(str(pdf_path))
for page in reader.pages:
    text = page.extract_text()  # plain text, no markdown
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: load_pdf_manuals() in loader.py + Wave 0 loader tests</name>
  <files>app/ingest/loader.py, tests/test_loader.py</files>
  <read_first>
    app/ingest/loader.py
    tests/test_loader.py
    raw_data/manuals/
  </read_first>
  <behavior>
    - load_pdf_manuals(directory: Path) -> list[RawDocument] globs *.pdf in directory (Path.resolve() first)
    - Returns documents sorted by filename (deterministic ordering)
    - Each RawDocument has source_type="pdf_manual", source_id=str(pdf_path.resolve()) (full absolute path — required so chunk_pdf() can re-open the file from any CWD), title=pdf_path.stem, content_hash=sha256 of NFKC-normalized full text
    - Uses pymupdf4llm.to_markdown(str(pdf_path), page_chunks=True) to extract all pages; joins page["text"] with "\n\n"; NFKC-normalizes and strips for content_hash computation
    - If pymupdf4llm raises any Exception: falls back to pypdf PdfReader — iterate reader.pages, call page.extract_text(), join with "\n\n", NFKC-normalize
    - If both pymupdf4llm and pypdf raise: logs WARNING with pdf_path.name and repr(e), skips to next file (does NOT raise)
    - test_load_pdf_manuals_source_type: calls load_pdf_manuals(MANUALS_DIR) and asserts all docs have source_type == "pdf_manual"
    - test_load_pdf_manuals_source_id_is_absolute_path: asserts all source_ids are absolute paths (os.path.isabs(doc.source_id)) and end with ".pdf"
    - test_load_pdf_manuals_count: asserts len == 15 (the 15 PDFs in raw_data/manuals/)
    - test_load_pdf_manuals_content_hash_is_hex64: asserts each content_hash is 64 lowercase hex chars
    - test_load_pdf_manuals_sorted_order: asserts sorted by source_id (load twice, same order)
  </behavior>
  <action>
    In app/ingest/loader.py: Add imports for logging, pymupdf4llm, and pypdf (PdfReader). Add a module-level logger = logging.getLogger(__name__). Add load_pdf_manuals(directory: Path) -> list[RawDocument] function following the load_forum_posts() pattern: Path.resolve() + sorted glob("*.pdf"). For each PDF, attempt pymupdf4llm.to_markdown(str(pdf_path), page_chunks=True); join page["text"] for all pages with "\n\n"; NFKC-normalize + strip; sha256 for content_hash. On exception: try pypdf fallback (PdfReader, iterate pages, extract_text(), join "\n\n", same normalization). On double failure: logger.warning("PDF load failed %s: %r", pdf_path.name, e); continue. title = pdf_path.stem. CRITICAL: source_id=str(pdf_path.resolve()) — must be the full absolute path so chunk_pdf() can call pymupdf4llm.to_markdown(raw_doc.source_id, ...) from any working directory without FileNotFoundError. Return sorted list (sorted by source_id).

    In tests/test_loader.py: Add MANUALS_DIR = Path(__file__).resolve().parent.parent / "raw_data" / "manuals". Add the 5 test functions listed in the behavior block. These tests exercise the real PDF files in raw_data/manuals/ — no mocking needed since the files are on disk. All tests are offline (no network calls).
  </action>
  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor && venv/bin/python -m pytest tests/test_loader.py -k "pdf" -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 PDF loader tests pass; load_pdf_manuals(Path("raw_data/manuals")) returns exactly 15 RawDocuments with source_type="pdf_manual"; no test requires network access; pytest tests/test_loader.py -k pdf -x exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chunk_pdf() in chunker.py + chunker tests</name>
  <files>app/ingest/chunker.py, tests/test_chunker.py</files>
  <read_first>
    app/ingest/chunker.py
    tests/test_chunker.py
    app/ingest/loader.py
  </read_first>
  <behavior>
    - chunk_document() dispatch: add elif raw_doc.source_type == "pdf_manual": return chunk_pdf(raw_doc) before the NotImplementedError raise
    - chunk_pdf(raw_doc: RawDocument) -> list[Chunk] uses pymupdf4llm.to_markdown(raw_doc.source_id, page_chunks=True, pages=list(range(1, n_pages))) — raw_doc.source_id is a full absolute path (set by load_pdf_manuals()), so this call succeeds from any CWD; falls back to pypdf PdfReader(raw_doc.source_id) on exception
    - ToC skip heuristic (D-04): for each page, count lines where len(line.split()) <= 4; if >50% of non-empty lines meet this condition, skip the page
    - Heading detection (D-01): line matching re.match(r"^#{1,6}\s+(.+)$", line.rstrip()) is a new section boundary; current_heading updates to the detected heading text; pre-heading content uses section_heading=""
    - Table-atomic rule (D-06): a "table block" is a maximal contiguous run of lines starting with "|". When the greedy accumulator would close a chunk and the current block starts with "|", defer the chunk boundary until the entire table block (all consecutive |-lines) is accumulated. Tables are never split across chunks.
    - Within each section: greedy paragraph-packing identical to chunk_forum() — 300-500 token windows (MAX_TOKENS=500), separator overhead of _SEPARATOR_TOKENS=1 per joined paragraph
    - Each Chunk metadata carries: source_filename=Path(raw_doc.source_id).name (filename only, for display — D-03), section_heading=str (D-03), page_number=int 1-based (D-03)
    - pypdf fallback: if pymupdf4llm raises, use pypdf PdfReader on raw_doc.source_id; plain text, no heading detection; all chunks get section_heading=""
    - test_pdf_chunker_no_table_split: constructs a synthetic markdown string with a GFM table block spanning 20 rows (token count would force split mid-table in naive implementation); asserts no chunk has a | as its last line without a corresponding table header line
    - test_pdf_chunker_skips_cover: synthetic page 0 contains "User Manual Cover Page" text; asserts no chunk contains that text
    - test_pdf_chunk_metadata: asserts every chunk has "section_heading" and "page_number" keys in metadata
    - test_pdf_chunker_dispatch: chunk_document(RawDocument(source_type="pdf_manual", ...)) does NOT raise NotImplementedError
    - test_pdf_chunks_within_token_budget: all chunks have token_count <= MAX_TOKENS (500)
  </behavior>
  <action>
    In app/ingest/chunker.py: Add imports for pymupdf4llm, pypdf (PdfReader), logging, and re (if not already present). Add module-level _HEADING_RE = re.compile(r"^#{1,6}\s+(.+)$"). Add _is_toc_page(lines: list[str]) -> bool helper: count non-empty lines where len(line.split()) <= 4; return (count / max(total_non_empty, 1)) > 0.5. Add _is_table_line(line: str) -> bool: return line.lstrip().startswith("|"). Add chunk_pdf(raw_doc: RawDocument) -> list[Chunk]:

    Implementation approach: call pymupdf4llm.to_markdown(raw_doc.source_id, page_chunks=True, pages=list(range(1, n_pages))) where n_pages = len(all_pages) from a first call without pages filter (or use pypdf to count pages). For each page dict (sorted by page["metadata"]["page_number"]): skip page 0 (already handled by pages= parameter). Apply ToC heuristic. Within the page text, split on line endings and process line-by-line: heading lines update current_heading and flush current accumulator into a chunk; table blocks (consecutive |-lines) are treated as one atomic block and never split mid-table; other lines form paragraph blocks that obey the greedy token budget. Each finalized chunk gets metadata={"source_filename": Path(raw_doc.source_id).name, "section_heading": current_heading, "page_number": page_num} — use Path(raw_doc.source_id).name (filename only) for the display field per D-03.

    For the table-atomic guarantee: when building block lists, collect consecutive |-lines into one block string (joined with "\n"). This block enters the greedy accumulator as a single unit. The greedy accumulator MUST NOT split this block. If adding the table block would exceed MAX_TOKENS AND there are already blocks in current: emit the current (without the table block), then start a new chunk beginning with the table block. The table block itself is never split.

    Add elif raw_doc.source_type == "pdf_manual": return chunk_pdf(raw_doc) in chunk_document() before the NotImplementedError.

    In tests/test_chunker.py: Add the 5 test functions. For test_pdf_chunker_no_table_split and test_pdf_chunker_skips_cover: create synthetic RawDocument with source_type="pdf_manual" and source_id pointing to a real PDF or use monkeypatching of pymupdf4llm.to_markdown to return controlled page dicts. The cleanest approach: mock pymupdf4llm.to_markdown at the test level using monkeypatch to return a controlled list of page dicts with synthetic text containing a large table and verify no chunk boundary falls mid-table.
  </action>
  <verify>
    <automated>cd /Users/yashvinaykumar/Desktop/guitar-tone-advisor && venv/bin/python -m pytest tests/test_chunker.py -k "pdf" -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 PDF chunker tests pass; test_pdf_chunker_no_table_split verifies table atomicity; chunk_document(RawDocument(source_type="pdf_manual",...)) no longer raises NotImplementedError; every chunk metadata has section_heading and page_number; pytest tests/test_chunker.py -k pdf -x exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw_data/manuals/ filesystem | PDF files read from committed directory; Path.resolve() + glob("*.pdf") prevents path traversal |
| pymupdf4llm output | PDF content treated as untrusted text — no code execution; text is stored verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | PDF path glob | mitigate | Path.resolve() + glob("*.pdf") identical to load_forum_posts() pattern (T-02-01 mitigated in Phase 1) |
| T-06-04 | Denial of Service | Malformed PDF in manuals/ | accept | pymupdf4llm raises on corrupt PDF; exception caught, WARNING logged, file skipped — pipeline continues |
| T-06-05 | Information Disclosure | PDF metadata leaking sensitive content | accept | Single-user local tool; PDFs are committed to repo; no PII concern |
</threat_model>

<verification>
Static checks (no Postgres, no network):
  pytest tests/test_loader.py -k "pdf" -x -v
  pytest tests/test_chunker.py -k "pdf" -x -v

Dispatch check:
  grep -n "pdf_manual" app/ingest/chunker.py
  # Must show the elif branch routing to chunk_pdf()

Table atomicity grep:
  grep -n "_is_table_line\|table" app/ingest/chunker.py
  # Must show table detection logic

Full unit suite (all offline tests):
  pytest tests/test_loader.py tests/test_chunker.py -x -v 2>&1 | grep -E "passed|failed|error"
</verification>

<success_criteria>
- load_pdf_manuals(Path("raw_data/manuals")) returns 15 RawDocuments all with source_type="pdf_manual" and source_id as os.path.isabs() == True paths
- chunk_document(RawDocument(source_type="pdf_manual",...)) calls chunk_pdf() without raising
- chunk_pdf() never produces a chunk whose text ends with a | line that is the start of an unclosed table
- Every PDF chunk metadata contains "section_heading" (str) and "page_number" (int) keys
- pytest tests/test_loader.py -k pdf -x exits 0
- pytest tests/test_chunker.py -k pdf -x exits 0
- Full unit test suite: pytest tests/test_loader.py tests/test_chunker.py -x exits 0
</success_criteria>

<output>
After completion, create /Users/yashvinaykumar/Desktop/guitar-tone-advisor/.planning/phases/06-full-corpus-ingestion/06-02-SUMMARY.md
</output>

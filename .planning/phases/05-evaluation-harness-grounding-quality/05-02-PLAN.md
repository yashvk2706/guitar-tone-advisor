---
phase: 05-evaluation-harness-grounding-quality
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/test_eval_refusal.py
autonomous: true
requirements: [EVAL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "An offline test confirms the refusal-assertion logic fires when given a fabricated (non-refusal) response"
    - "An offline test confirms that an empty-context refusal response passes the refusal-phrase assertion"
    - "An offline adversarial test confirms a mismatched-context response asserts the refusal phrase AND contains no knob-setting pattern"
    - "A live integration test (skipped unless ANTHROPIC_API_KEY set) confirms the real model refuses with empty context"
    - "All refusal tests call stream_response() DIRECTLY — never via the HTTP endpoint (Pitfall 5)"
  artifacts:
    - path: "tests/test_eval_refusal.py"
      provides: "4 refusal smoke tests: 3 offline unit + 1 live integration"
      contains: "def test_empty_context_produces_refusal"
  key_links:
    - from: "tests/test_eval_refusal.py"
      to: "app/generation/generator.py::stream_response"
      via: "asyncio.run(_collect(client, sources_with_ids))"
      pattern: "stream_response\\("
    - from: "tests/test_eval_refusal.py"
      to: "app/generation/prompt.py"
      via: "build_system_blocks() + build_messages()"
      pattern: "build_system_blocks|build_messages"
    - from: "tests/test_eval_refusal.py adversarial case"
      to: "app/retrieval/base.py::ChunkResult"
      via: "inline lo-fi ChunkResult constructed as (chunk, sn) tuples"
      pattern: "ChunkResult\\("
---

<objective>
Build the refusal contract smoke tests (`tests/test_eval_refusal.py`) that fail loudly if the generation layer ever fabricates an answer when the corpus is empty or adversarially mismatched.

Purpose: Enforce GEN-06's refusal contract automatically forever. The empty-context and wrong-context cases are the two ways a RAG system silently degrades into a hallucinating chatbot; these tests turn that failure mode into a red build.
Output: `tests/test_eval_refusal.py` (4 tests: 3 offline unit + 1 live-gated integration). No production code — this plan asserts existing generation-layer behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evaluation-harness-grounding-quality/05-CONTEXT.md
@.planning/phases/05-evaluation-harness-grounding-quality/05-RESEARCH.md
@.planning/phases/05-evaluation-harness-grounding-quality/05-PATTERNS.md

# Source files the executor MUST treat as the contract:
@app/generation/generator.py
@app/generation/prompt.py
@app/retrieval/base.py
@tests/test_generation.py

<interfaces>
<!-- Exact contracts extracted from the codebase. Use these directly. -->

From app/generation/generator.py:
- `async def stream_response(*, client: AsyncAnthropic, system_blocks: list[dict], messages: list[dict], sources_with_ids: list[tuple[ChunkResult, int]], session_id: str) -> AsyncIterator[ServerSentEvent]`
- Event sequence: `event="session"` first → plain `data:` token events with `{"text": "<token>"}` (sse.event is None) → `event="citations"` last with `{"sources": [...]}`
- Token text is collected via: `if sse.event is None: payload = json.loads(sse.data); if "text" in payload: parts.append(payload["text"])`

From app/generation/prompt.py:
- `def build_system_blocks() -> list[dict]`
- `def build_messages(turns: list[dict], user_message: str, sources_with_ids: list[tuple[ChunkResult, int]]) -> list[dict]`
- SYSTEM_PROMPT_TEXT rule 2 contains the exact refusal phrases: `"I don't have material"` and `"the closest I have"`

From app/retrieval/base.py:
- `class ChunkResult` frozen dataclass: chunk_id, document_id, source_type, source_name, chunk_index, text, distance

CRITICAL pitfall (Pitfall 5 / Pitfall 6):
- The fake client method is `.messages.stream(**kwargs)` returning a `_FakeAnthropicStream` whose `.text_stream` property yields tokens (copy from test_generation.py lines 61–98). Do NOT route through the HTTP endpoint — test_main.py uses `raise_server_exceptions=False` which silently swallows the `sources=` vs `sources_with_ids=` TypeError.
- stream_response wraps sources as `list[tuple[ChunkResult, int]]` — wrap each lo-fi chunk as `(chunk, sn)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — write failing refusal test stubs (RED)</name>
  <files>tests/test_eval_refusal.py</files>
  <read_first>
    - tests/test_generation.py (_FakeAnthropicStream/_FakeAnthropicClient/_FakeMessages lines 61–98, _reset_sessions autouse fixture lines 105–123, asyncio.run() in sync test pattern lines 214–226)
    - app/generation/generator.py (stream_response signature + SSE event shape — confirms `sse.event is None` carries `{"text": ...}`)
    - app/generation/prompt.py (build_system_blocks, build_messages signatures; SYSTEM_PROMPT_TEXT refusal phrases lines 43–44)
    - app/retrieval/base.py (ChunkResult field names lines 38–62)
    - .planning/phases/05-evaluation-harness-grounding-quality/05-PATTERNS.md (the `tests/test_eval_refusal.py` section lines 282–387)
  </read_first>
  <action>
    Create tests/test_eval_refusal.py with 4 tests in RED state using exact names from 05-VALIDATION.md: test_empty_context_refusal_assertion, test_empty_context_produces_refusal, test_adversarial_mismatch_no_knobs, and one `@pytest.mark.integration` live test gated on an `anthropic_key` fixture. Copy VERBATIM from test_generation.py: the _FakeAnthropicStream + _FakeAnthropicClient + _FakeMessages classes (lines 61–98) and the _reset_sessions autouse fixture (lines 105–123). Add an `anthropic_key` fixture that does `pytest.skip("ANTHROPIC_API_KEY not set ...")` when `os.environ.get("ANTHROPIC_API_KEY")` is falsy. Define module-level constants: `REFUSAL_PHRASES = ("I don't have material", "the closest I have")`, `_KNOB_RE = re.compile(r"[A-Za-z][A-Za-z\s]{0,15}[:=]\s*\d+(?:\.\d+)?")`, `_FABRICATED_EVH_RESPONSE = "Set your amp to Gain=7, Bass=6, Mid=4, Treble=8. Use the 5150 lead channel for maximum brown sound crunch."`. Define an async helper `_collect(client, sources_with_ids, user_message="Get EVH gain tone")` that drains stream_response (imported inside the function) using `build_system_blocks()` + `build_messages([], user_message, sources_with_ids)`, session_id="test-refusal", appending `json.loads(sse.data)["text"]` only when `sse.event is None and "text" in payload`; return "".join(parts). Each test imports app modules inside its body. test_empty_context_refusal_assertion: fake streams _FABRICATED_EVH_RESPONSE with sources_with_ids=[]; assert that the refusal-phrase check is FALSE for this fabricated text AND that _KNOB_RE.findall finds matches (proves the negative-assertion machinery detects fabrication — `assert not any(p in text for p in REFUSAL_PHRASES)` and `assert _KNOB_RE.findall(text)`). test_empty_context_produces_refusal: fake streams `["I don't have material on EVH — the closest I have is the Marshall JCM800"]` with sources_with_ids=[]; assert `any(p in text for p in REFUSAL_PHRASES)`. test_adversarial_mismatch_no_knobs: construct one lo-fi ChunkResult (chunk_id="11547f9a-2b6b-4074-a301-80113ee72bea", document_id="doc-lofi-1", source_type="forum", source_name="lo_fi_tone.txt", chunk_index=0, text="For lo-fi bedroom guitar tone, use a cheap amp mic'd badly with tape hiss.", distance=0.6) wrapped as `[(chunk, 1)]`; fake streams a refusal token list (e.g. `["I don't have material on the EVH brown sound — the closest I have is lo-fi bedroom tone, want that instead?"]`); assert `any(p in text for p in REFUSAL_PHRASES)` AND `_KNOB_RE.findall(text) == []` (no fabricated settings). The live integration test (uses anthropic_key fixture): construct a real AsyncAnthropic client with the key, call `_collect` with sources_with_ids=[] and the EVH query, assert `any(p in text for p in REFUSAL_PHRASES)`. Use `asyncio.run(_collect(...))` inside every sync test body (no pytest-asyncio — Pitfall 3). Import stubs use `# type: ignore[import-not-found]` only where a not-yet-existing symbol is referenced; since all production code already exists, these tests should be runnable immediately — RED here means the offline tests are written and the integration test SKIPS.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest tests/test_eval_refusal.py -q --tb=short 2>&1 | grep -E "error|failed|passed|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python -m pytest tests/test_eval_refusal.py --collect-only -q` lists exactly 4 tests: test_empty_context_refusal_assertion, test_empty_context_produces_refusal, test_adversarial_mismatch_no_knobs, and the integration test
    - The integration test is SKIPPED when ANTHROPIC_API_KEY is unset (verify: run with the var unset, see `1 skipped`)
    - `grep -c "TestClient\|httpx\|/chat" tests/test_eval_refusal.py` returns 0 — tests never go through the HTTP endpoint (Pitfall 5)
    - `grep -c "asyncio.run" tests/test_eval_refusal.py` returns ≥ 3 (one per offline async-collecting test + integration)
    - The _FakeAnthropicClient class body matches test_generation.py lines 61–98 (has `.messages.stream()`, not `.create()`)
  </acceptance_criteria>
  <done>tests/test_eval_refusal.py exists with 4 collectable tests; integration test skips without the API key; no HTTP-endpoint usage.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the 3 offline refusal tests pass against the existing generation layer (GREEN)</name>
  <files>tests/test_eval_refusal.py</files>
  <read_first>
    - tests/test_eval_refusal.py (the tests written in Task 1)
    - app/generation/generator.py (confirm the SSE token shape the _collect helper depends on)
  </read_first>
  <action>
    Run the 3 offline tests against the EXISTING app/generation/generator.py + app/generation/prompt.py (no production code changes expected — this plan validates an existing contract). If any offline test fails, the failure is in the test wiring, not the generator: diagnose via the _collect helper (confirm it filters on `sse.event is None` and reads `payload["text"]`, matching generator.py line 97 `yield ServerSentEvent(data=json.dumps({"text": text}))`), confirm build_messages is called with the 3 positional args `([], user_message, sources_with_ids)`, and confirm sources_with_ids is `list[tuple[ChunkResult, int]]` (not a bare ChunkResult list). Do NOT modify generator.py or prompt.py to make a test pass — if a genuine generator bug surfaces, STOP and surface it in the SUMMARY rather than weakening the assertion. Fix only the test file. Ensure the knob-regex constant and refusal-phrase tuple exactly match the RESEARCH.md / D-09 values so the assertions are meaningful.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest tests/test_eval_refusal.py -k "not integration" -q --tb=short 2>&1 | grep -E "failed|passed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python -m pytest tests/test_eval_refusal.py -k "not integration" -q` reports 3 passed, 0 failed
    - test_empty_context_refusal_assertion proves the negative path: the fabricated text fails the refusal check and trips _KNOB_RE (the assertion machinery works)
    - test_adversarial_mismatch_no_knobs proves the refusal response contains zero knob-setting matches
    - No edits to app/generation/generator.py or app/generation/prompt.py were made (verify: `git diff --stat app/generation/` is empty)
  </acceptance_criteria>
  <done>3 offline refusal tests pass; integration test skips; no production code modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empty/mismatched retrieval → generation | The contract under test: zero or wrong context must yield a refusal, not fabricated gear/settings |
| test fake client → stream_response | Fake streams pre-canned tokens; must exercise the real generator code path, not a mocked HTTP layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing (fabricated grounding) | app/generation/generator.py refusal contract | mitigate | Smoke tests assert refusal phrases for empty + adversarial context and assert no knob-setting pattern leaks; run in every pytest invocation |
| T-05-07 | Tampering (silent test suppression) | test transport path | mitigate | Tests call stream_response() directly via asyncio.run(); never via TestClient with raise_server_exceptions=False (Pitfall 5) — enforced by acceptance criterion grep |
</threat_model>

<verification>
- `venv/bin/python -m pytest tests/test_eval_refusal.py -k "not integration" -q` → 3 passed
- `venv/bin/python -m pytest tests/test_eval_refusal.py -q` (no key) → 3 passed, 1 skipped
- `venv/bin/python -m pytest tests/ -q` → no regressions
- `git diff --stat app/generation/` → empty (this plan adds tests only)
- Manual (live, requires ANTHROPIC_API_KEY): `ANTHROPIC_API_KEY=... venv/bin/python -m pytest tests/test_eval_refusal.py -m integration -q` → real model refuses with empty context
</verification>

<success_criteria>
- Empty-context refusal asserted via exact SYSTEM_PROMPT_TEXT phrases
- Adversarial mismatch asserts refusal AND zero knob-setting matches
- All tests exercise stream_response() directly (no HTTP path)
- Live integration test gated cleanly on ANTHROPIC_API_KEY
- No production code changed
</success_criteria>

<output>
After completion, create `.planning/phases/05-evaluation-harness-grounding-quality/05-02-SUMMARY.md`
</output>
